# Ariadne——LLM DBA管理与目的驱动的联想记忆检索系统 对比评测数据


---

## 摘要

长期记忆系统有两个相互依赖的子问题：前置数据管理（如何将对话日志持续转化为结构化记忆图谱）与在线检索（如何召回与查询相关的记忆）。《理论部分》分别提出了 **LLM DBA 架构**（将记忆图谱视为 LLM 自主维护的数据库，支持增量纠错）与**目的驱动的联想记忆检索模型 PAR**（跳转轴 + 目的回归 + 峰值容忍带），并在人工构造、精确标注的 47/63/83/216 节点数据集上完成了机制验证。本文延续这一工作，转向**生态效度与端到端评测**：在自建的 322 节点个人记忆数据集 LiFEMem 与 35 条陈述式查询上，完成检索层与故事质量层的多系统对照（PAR / Naive RAG / Mem0 / Graphiti），并对 LLM DBA 自动建图环节进行子集（100 节点）与全量（322 节点）闭环评测。

主要发现可概括为六条（**以下结论均限定在人工构建的纯情景记忆数据集的条件下**，即 LiFEMem 322 节点自建图谱与配套陈述式查询集；外推至真实规模、非人工标注的记忆数据需谨慎）：

1. **评测范式决定胜负**。同一套 PAR 与 RAG 系统，问答框架下 RAG 胜（5 维总分 4.67 vs 4.51），记忆框架（纯故事对比、无查询）下 PAR 以 25:5（83.3%）碾压。带查询的 judge 天然把联想当噪音——评测框架越公平，PAR 的优势越明显。
2. **检索层纯向量可能是记忆检索的最优默认方案**。三系统检索层 expected 覆盖：RAG/Mem0（纯向量）0.840 > PAR（图谱）0.765 > Graphiti（图谱）0.642；BM25 中文失效（Mem0 接入 35/35 逐条退化为纯向量），图谱仅在联想检索维度有价值（PAR 纯故事记忆价值全面占优）。注：该结论收敛为「可能」级——联想/发散价值难以度量、关键词与图遍历当前无增益且混合检索部署成本高（论证见 §5.1）。
3. **外部框架的主指标归因收口**。T6 五维盲评（带查询目的）Graphiti 4.74 > RAG 4.67 > PAR 4.51，与检索层排位完全反转——Graphiti 的"事实级检索集中效应 + 抽取合并信息收窄"使其在查询贴合上占优、在记忆保真上垫底（纯故事 26:4 落后 PAR）。
4. **DBA 抽取是提纯驱动的双重压缩**，边缺失 70.3% 归因于节点缺失（下游果而非 LLM 连边保守）；经"两步拆分 + 激进连边"改造后节点召回 75–76%、边召回 35%、边类型一致率 77%。
5. **图结构一致性要求低**：gold 人工图与 DBA 自动图边重合率仅 34.8%，但 4 次独立抽取的纯故事总分恒定 4.725、全部高于 gold——DBA 自动建图不劣于人工 gold 图，记忆价值维度反超。
6. **联想链路的前置性（收官定论）**：联想的"原料"在抽取层的边连接就已确定，检索只是利用边结构（补连实验 0/+3.7pp 的初步证据）；共识区（两图共同命中）是纯向量语义下限，发散区是图结构增量上限——带查询测语义收敛、纯故事测联想丰富，两范式各度量检索的一个属性。

本文亦明确两项错误设计与一项概念定论：**拒答机制是错误设计**（真实使用中主 LLM 自主决定不调用记忆工具即天然弃权）；**Mem0 图谱功能不引入**（依赖英文启发式，中文下可预见与 BM25 同一下场）；**知识库 ≠ 记忆系统**（情景记忆/海马体 vs 语义记忆/颞叶新皮层，应分离合作而非堆叠检索方法）。

---

## 一、评测基础设施与数据集

### 1.1 数据集：LiFEMem（322 节点 / 410 边）

评测基于自建的个人记忆数据集 **LiFEMem**（《0818 自建个人记忆数据集评测计划》）：以 216 节点/266 边的初始图谱为基础，扩展"学生时代"（高中学习、同桌与友情、早恋、大专迷失、社团与转码）、家庭、宠物、旅行、消费等场景，并引入**时序异质化记忆节点**（同主题不同时间的演进与矛盾记忆，三种表达：content 内嵌时间、deprecated 标记、TEMPORAL 边）。最终规模：

- **322 节点 / 410 边**，单一自然人从高中 → 大专 → 工作 → 当下的完整叙事（家庭：父刘建国/母王秀兰/妹刘雨桐；同学：同桌林晓雨/死党陈浩/恋人苏婉婷）；
- 边类型 7 种：CAUSAL(168) / SCENARIO(104) / SOCIAL(55) / SEQUENCE(49) / ATTRIBUTE(19) / TEMPORAL(8) / PREFERENCE(7)（TAXONOMIC 按跳转轴设计抑制）；
- 噪声节点 60 个（其中孤立噪声 38 个，占全部节点的 11.9%），用于评测噪声过滤能力（目前仅 DBA triage 侧证，检索/故事层未系统评测，见 §6.3）；
- 9 条时序演进线（咖啡戒断、职业、运动、居住地、学习方向、早恋、兼职→工作等），保证 PAR 检索能沿「成绩好→下滑→高考压力→分手→高考失利→大专迷失→走出困惑→转码」的因果链走通。

### 1.2 查询集：三版演进（本身即实验发现）

| 版本 | 形态 | 构成 | 说明 |
|------|------|------|------|
| v1 | 问答式 | 35 条 7 类 + expected | 「用户现在做什么工作？」 |
| v3 | **陈述触发式** | 35 条 = 30 陈述 + 5 弃权，9 类情境 | 「最近项目排得太满了，天天加班到十一二点……」 |

7 类查询：single_fact(6) / preference(5) / multi_hop(5) / knowledge_update(4) / temporal(5) / cross_scene(5) / abstention(5)。范式认知：真实对话是"我/你"关系——用户**表达状态**而非明确提问，记忆系统在**无明确提问**语境下触发「陈述 → 目的推断 → 联想检索 → 记忆 → 回应」。

### 1.3 评测范式四轮修正（方法论的自我进化）

| 轮次 | 修正内容 | 实证效果 |
|------|----------|----------|
| 初版 | 问答式查询 + 五维盲评（StoryRank 统一故事化 + DeepSeek 1–5 盲评） | 暴露 PAR 事实查询目的推断跑偏（「喜欢喝什么咖啡」被推断为「社交互动」） |
| 第一轮 | `PURPOSE_INFERENCE_PROMPT` 类型化（information/emotional） | q07 目的「社交互动」→「确认细节」，推断层噪声消除 |
| 第二轮 | 评判口径与检索范式匹配：noise_filter 基于查询目的判定 + 恢复 CROSS_VALID | 解决"查询字面与目的扩展口径不一致"矛盾 |
| 第三轮 | 陈述触发式 v3（查询重构为用户第一人称陈述） | 目的推断 30/30 精准；PAR 差距收窄（4.53→4.51 vs RAG 4.74→4.67） |
| 第四轮 | **三框架评测**（决定性）：问答五维 / 偏好对比 / 纯故事对比 | 发现带查询评测受 judge 认知偏置，引入无查询框架 |

### 1.4 三框架评测体系

| 评测框架 | 判定依据 | 结果 |
|----------|----------|------|
| 问答框架·五维（带陈述） | 主题相关性 | RAG 胜（4.67 vs 4.51） |
| 问答框架·偏好（带陈述） | 主题相关性 | RAG 略优（16:14） |
| **记忆框架·纯故事对比（无查询）** | **记忆资产质量** | **PAR 胜（25:5，83.3%）** |

### 1.5 公平性协议（五同）

外部框架对照遵循**公平性五同**：同数据（LiFEMem 322 节点 content 铺平为 322 条记忆条目，含 deprecated/forgotten）、同查询（35 条统一）、同指标（StoryRank 五维 + R@all/P@5/CROSS_OUT）、同判定（DeepSeek 盲评统一应用）、同 embedding（bge-large-zh-v1.5）。候选配额以 PAR 输出量 N 动态对齐；主指标为故事质量评价（不预设正确答案），检索层指标仅作辅助诊断。方法论声明如实披露数据集构造偏差（expected 沿图邻接节点反推、方向偏向 PAR——该偏差已针对 DBA 侧修复：为 DBA 图独立重标 expected，见 §4.2 口径声明）与边的信息不对称（仅 PAR 加载人工 gold 图，但有反证：LiFEMem 人工图一次性粗略构造，无 DBA 持续纠错，信息保真度不高于 DBA 自动维护图）。

### 1.6 judge 认知偏置（核心方法论发现）

**LLM judge 不懂联想价值。** ① judge 判断相关性用主题/字面匹配，联想内容（加班→脂肪肝）因主题不同被判"偏离"；② RAG 输出天然贴合这种判定（按字面语义检索必然"相关"）——judge 的相关性标准是 RAG 形状的；③ 同一个 PAR 故事，问答框架里是噪音（noise_filter 3 分）、记忆框架里是财富（83% 胜）。**带查询的 QA 式评分会系统性低估联想型记忆系统。**

---

## 二、评测范式与主指标结果（0818）

### 2.1 v3 故事质量五维（30 条非弃权）

| 系统 | relevance | coherence | completeness | faithfulness | noise_filter | total |
|------|:---:|:---:|:---:|:---:|:---:|:---:|
| PAR | 4.70 | 4.47 | 4.47 | 4.97 | 3.93 | **4.51** |
| Naive RAG | 4.73 | 4.70 | 4.53 | 4.97 | 4.43 | **4.67** |

差距几乎全部来自 **noise_filter（−0.50）** 与 coherence（−0.23）；faithfulness 双方持平（4.97，均无编造）。本质：PAR 是"联想检索长故事"（信息多但噪声多，平均 462 字 vs RAG 358 字），RAG 是"聚焦短故事"。

### 2.2 分情境对比（v3，total 均值）

| 情境 | PAR | RAG | 说明 |
|------|:---:|:---:|------|
| 健康(3) | **5.0** | 4.8 | PAR 唯一稳定领先 |
| 生活(4) | **4.05** | 3.9 | 双方共同最弱项 |
| 学习(3) | 4.6 | 4.6 | 持平 |
| 其余 7 类 | 落后 0.2–0.5 | — | 感情类差距最大（−0.46） |

### 2.3 纯故事对比·分情境胜率（记忆框架，决定性）

| 情境 | 胜负 | 情境 | 胜负 |
|------|:---:|------|:---:|
| work_pressure(4) | 4:0 | emotion(4) | 3:1 |
| health(3) | 3:0 | nostalgia(4) | 3:1 |
| life(4) | 4:0 | relationship(3) | 2:1 |
| future(2) | 2:0 | learning(3) | 2:1 |
| — | — | social(3) | 2:1 |
| **合计** | **25:5（83.3%）** | | |

**没有任何一个情境 RAG 能成规模压制 PAR。** 判定理由高频词：纯故事框架——"具体/细节"28/30、"维度/多维"22/30、"连贯/衔接"15/30、"丰富/信息量"14/30（标尺=信息丰富度）；偏好框架——"当下/当前/此刻"25/30、"贴合"23/30（标尺=贴合当下处境）。

### 2.4 偏好对比·分情境胜率（带陈述问答框架）

合计 **14:16 RAG 略优**。关键翻转：work_pressure 4/4→1/3、nostalgia 3/4→1/3、**emotion 3/4→0/4、social 2/3→0/3**。规律：**PAR 在"无明确当前痛点"情境胜（联想=资产），在"有明确当下诉求"情境输（联想=冗余）**——同一批故事仅因 judge 是否带"当下陈述"，PAR 的 25 胜坍缩到 14 胜，是评测范式偏差最硬核的对照证据。

### 2.5 检索层辅助指标（30 条非弃权）

| 系统 | expected 覆盖 | CROSS_OUT | 平均输出量 |
|------|:---:|:---:|:---:|
| PAR | 0.805 | 0.656 | 40 |
| Naive RAG | 0.865 | 0.634 | 40 |

> 注：本表为 0818 两系统轮次数字（候选输出均值 40）；0819/0821 三系统轮次将候选配额对齐至均值 38.3 后，PAR/RAG 的 expected 覆盖为 **0.765 / 0.840**（CROSS_OUT 不变，见 §3.4）——两处数字并存源于评测批次不同，后续引用以三系统轮次为准。

expected 覆盖衡量"与陈述字面相关的节点"的字面召回，RAG 语义匹配天然擅长；**PAR 的独特联想价值在 expected 之外**（s01 实证：RAG 候选 top-41 没有脂肪肝/缓解方式，无论 top-N 多大都进不来——语义相似度不足；PAR 经跳转轴「加班→健康→情绪→缓解」触及）。

### 2.6 方法论论证闭环（六点）

1. **召回优先**：检索层宁多勿缺——false positive 代价低（下游可滤），false negative 代价高（永久丢失）；
2. **检索层 ≠ 最终回复**：管道为 检索（高召回）→ StoryRank 故事化 → 主聊天 LLM 按上下文过滤生成——用最终回复质量反推检索层质量是评测误用；
3. **PAR 多出的部分 = RAG 触及不到的记忆**：s01 中 PAR 独有的联想节点（脂肪肝、缓解方式）在向量空间与查询相似度不足，是**向量检索架构上无法触及**；
4. **噪音可优化，漏召回不可修复**：PAR 的候选噪音可收紧容忍带优化，RAG 的联想漏召回是语义匹配架构决定的；
5. **反向论证**：PAR 承受"问答框架 + noise_filter 把联想当噪音"双重不利，仍只落后 0.16——被系统性惩罚仍逼近满分对手，证明真实质量远超评测所示；
6. **弱点专析**：见 §2.7。

### 2.7 弱点专析：PAR 最差四条（q17 / q27 / q18 / q19）

| 查询 | 类别 | expected 覆盖 | PAR total | RAG total | 失分机制 |
|------|------|:---:|:---:|:---:|------|
| q17 高中成绩变化 | knowledge_update | 1/2 | **3.0** | 5.0 | h0（高一名列前茅）完全未召回，联想发散到早恋/失眠/晚自习 |
| q27 朋友圈子形成 | cross_scene | **0/5** | **3.0** | 5.0 | 47 条候选全部偏"情绪低谷—迷茫—工作不满"轴，合理率仅 0.4 |
| q18 公司待遇 | knowledge_update | 1/1(排第10) | **3.2** | 4.2 | n37 命中但排第10，联想发散到"转行 AI 学习材料"（合理率 0.3） |
| q19 还打游戏吗 | knowledge_update | 1/1(排第41) | **3.4** | 5.0 | so4 排第41，联想先到加班再桌游社交再高中回忆（690 字大杂烩） |

共性：3/4 条是 knowledge_update，全部 noise_filter=2，expected 节点被联想稀释至排名靠后甚至缺失。**规律：PAR 在"联想向前推进"（表达当下压力）场景强、在"回溯特定时点事实"（现在还是不是）场景弱**——暴露 knowledge_update 时点对比、联想向后回溯两大待补机制。

### 2.8 弃权类定论（错误设计，不做实现）

弃权类 5 条（陈述隐含图中不存在的信息）：正确拒答 PAR=1/5、RAG=0/5（v3）。PAR 的 1/5（s34 狗子掉毛）实为 judge 偶然误判（故事 512 字实质内容仍被判 refused=True），非系统机制。**结论：拒答机制是错误设计**——真实使用中主 LLM 自主决定不调用记忆工具即天然弃权，检索器/评测层无需"拒答"能力。弃权类查询保留在查询集，仅作检索层诊断。

---

## 三、外部框架对照评测（0819 Mem0 / 0821 Graphiti）

### 3.1 检索层收束背景

对主流记忆系统（Mem0/Letta/Zep/Graphiti/GBrain/Hindsight/AgentMemory/Engram）的调研结论：全部检索信号可归入三种已测基元——纯向量（已测，0.840 当前最优）、关键词 BM25（已证中文失效）、图遍历（已测 PAR 0.765 / Graphiti 0.642）。"新方案"均为三基元**组合 + reranker**，关键词流中文仍退化。**在本数据集（人工构建的纯情景记忆）条件下无质变新机制，不再引入对比评测，评测重心转入 DBA 抽取环节（§4）。**

### 3.2 Mem0 接入：中文下退化为纯向量（35/35 逐条同序）

Mem0 两种配置（faiss / qdrant+BM25）候选与 Naive RAG **35/35 逐条同序一致**——中文语境下 Mem0 检索 = 纯向量。三层根因：

| 根因 | 机制 | 证据 |
|------|------|------|
| BM25 对中文失效 | `Qdrant/bm25` 分词器**按空白切分**，中文整句哈希为单维 token | `'用户喜欢喝咖啡提神' → [839542716] 值 1.688`；中文 1 token vs 英文 6 tokens |
| 实体匹配对中文失效 | spaCy 英文 NLP 启发式（`isupper()`/PROPN/noun_chunks），且本次为双重空操作（未安装 + 中文不适用） | 35/35 同序证明实体信号一次未参与排序 |
| Mem0 最大特点在前置记忆管理而非检索 | `add(infer=True)` 的 LLM 抽取/去重/更新/实体链接；本评测 `infer=False` 原文入库绕过了核心机制 | 架构分析 |

**定位澄清**：Mem0 前置管理 ≈ Ariadne **LLM DBA 系统**（写入方对标），但服务对象不在同一层次，不做实验对标，改为理论对比——关键差异是**边**：DBA 的语义边（CAUSAL/SCENARIO/SEQUENCE 等）是 Mem0 没有的第一类对象，二者**互补而非竞争**。

**架构性推论**：即使接入中文分词器修复 BM25，Mem0 的"向量+关键词"双路召回仍无法触及 PAR 的联想额外召回——联想节点与查询字面、语义双零重合，唯有 PAR 的因果边跳转可达（s01 实证：PAR 候选 24/41 条不在 Mem0 中，含脂肪肝/晨跑）。

### 3.3 Mem0 英文对照实验：BM25 是"净负效"（2026-08-20）

| 发现 | 数据 |
|------|------|
| 英文下 BM25 真正生效 | 中文 35/35 同序 vs 英文 0/10 同序；稀疏 token 数 1 vs 6 |
| 但对 expected 覆盖**零增益** | qdrant/faiss 均 23/30，逐条命中集完全一致；改变的只是非 expected 边界候选（平均 Jaccard 0.787） |
| **净负效**（以语义相似度为基准） | faiss 平均相关 **−0.90** vs qdrant（BM25+向量）**−0.45**；qdrant 独有节点平均相似度 0.405 < faiss 0.467 |
| 实体图谱功能仍双重空操作 | 需 `add(infer=True)` + spaCy + 英文数据三条件齐备 |
| 前置管理完整链路 | 模拟抽取产物入库：expected 覆盖 0.767 → 0.800（+1/30）——**前置管理确有增益但边际**，抽取只是条目级改写，不改变检索机制，仍无法触及 PAR 的联想节点 |

**结论**：中文是无效、英文是负效——BM25 牺牲语义精度换词面召回，而记忆数据口语化、词面信号是噪声（BM25 设计目标是技术文档/知识库式长文本，非个人记忆口语）。短板不在"中文分词"而在**词面信号与口语化记忆数据根本不匹配**。

### 3.4 Graphiti 接入：工程适配与检索层垫底（0821）

**后端决策**：Kuzu 弃用（项目停止维护、0.29.x 后端多个未修 bug、FTS 中文弱）→ Neo4j 5.26-community（Docker `graphiti-neo4j`，bolt 7687）。

**LLM 适配（本次卡点主因）**：api.gpt.ge 的 `deepseek-v4-flash-0731` 是推理模型，默认开启 thinking 导致 content 为空/截断（reasoning 最长 29,963 字符，`max_tokens=8192` 必截断）。5 组参数实测确定 `extra_body={"thinking": {"type": "disabled"}}` 关闭 thinking：`reasoning_content` 归零、content 直出完整 JSON，**全量 322 条建图约 20 分钟**完成。

**检索层指标（30 条非弃权）**：

| 系统 | expected 覆盖 | CROSS_OUT | 平均输出量 |
|------|:---:|:---:|:---:|
| PAR | 0.765 | 0.656 | 38.3 |
| Naive RAG | 0.840 | 0.634 | 38.3 |
| Mem0（qdrant） | 0.840 | 0.634 | 38.3 |
| Graphiti（修复前/后） | 0.654 / 0.642 | 0.685 / 0.678 | 36.6 / 33.6 |

> 注：PAR/RAG/Mem0 行数字取自 0819 批次（候选配额对齐至均值 38.3），Graphiti 行取自 0821 批次；Mem0 与 Naive RAG **35/35 逐条同序、实质等价**，单列仅为展示接入结果。

**expected 损失归因**（每个未命中节点用 Neo4j 检查是否进图）：修复前 28 个未命中 = **57.1% 抽取丢失**（16 个，content 从未被 LLM 抽成 fact）+ **42.9% 检索遗漏**（12 个，在图中但未进 top-N）→ 召回天花板 0.802（65/81），即便检索完美仍低于 RAG 0.840。跨场景增益（+0.03~0.05）与 expected 损失（−0.11~0.19）**不是"精度换联想"的交换关系**。

**写入侧修复实验**：Graphiti 有原生持续抽取/修正能力，重入 14 条丢失原文后进图率升至 **100%**；但覆盖未随修复提升（0.654→0.642）——**检索排序（top-N 内）才是主导瓶颈**。结论限定于小批量补录场景，不构成全量写入可靠性断言（全量 322 条初建时确有 17% 写入丢失，修复机制只能补救不能替代写入质量）。

**中文适配缺口**：个别 fact 被输出为英文；Neo4j 全文索引对中文按单字 token 索引（无词级匹配，实际由向量+图遍历兜底）。

### 3.5 三系统主指标（T6 故事质量五维盲评）

对 PAR / Naive RAG / Graphiti 候选统一经 StoryRank 故事化 → DeepSeek 五维盲评（匿名，temperature=0）：

| 系统 | total | 相关性 | 连贯因果 | 完整性 | 真实性 | 噪音过滤 | 联想合理 cv |
|------|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| **Graphiti** | **4.74** | 4.73 | **4.87** | **4.60** | 4.97 | **4.53** | 0.89 |
| Naive RAG | 4.67 | 4.73 | 4.70 | 4.53 | 4.97 | 4.43 | 0.91 |
| PAR | 4.51 | 4.70 | 4.47 | 4.47 | 4.97 | 3.93 | 0.81 |

> 注：PAR/RAG 评分为 **0819 批次复用**（与 §2.1 同源），Graphiti 为 0821 批次——temperature=0 同协议，但跨会话 judge 仍存在轻微漂移风险（0821 报告已声明「PAR/RAG（08-19）与 Graphiti（08-21）分两次评」）；未整体重跑系 LLM 主观波动会引入新的 PAR/RAG 差距波动、且成本高（约 40 分钟）。

两两对比（total）：PAR vs Graphiti 3 胜/10 平/17 负；PAR vs RAG 6/7/17；RAG vs Graphiti 6/13/11。**主指标排位与检索层完全反转**——Graphiti 的事实级输出经故事化后依然完整可用，检索层的形态性低估在主指标下得到补偿。真实性三系统均 4.97（无编造）；评分压缩效应明显（total 均 ≥4.5，判别力集中在连贯/噪音两维）。

### 3.6 纯故事选择评测（无查询）——与五维盲评方向反转

不带查询、不带评分维度，同一查询对应的两系统故事匿名成对（A/B 随机换位，seed=42）交给 LLM，仅判断"作为长期记忆哪个质量更高"：

| 对比 | A 胜 | B 胜 | 平 | A 胜率 |
|------|:---:|:---:|:---:|:---:|
| **PAR vs Graphiti** | 26 | 4 | 0 | 0.867 |
| PAR vs Naive RAG（0819） | 25 | 5 | 0 | 0.833 |
| Naive RAG vs Graphiti | 23 | 6 | 1 | 0.767 |

**纯选择完整排位：PAR > Naive RAG > Graphiti**（单调一致，与信息丰富度正相关）。Graphiti 仅 s08/s14（情绪/怀旧类）稳定胜出；三轮共 90 次对照仅 1 平。

**与五维盲评完全反转**：五维（带查询目的）Graphiti 4.74 > RAG 4.67 > PAR 4.51；纯选（无查询）PAR > RAG > Graphiti。一致性佐证：PAR 噪音过滤 3.93 五维最低、cv 0.81 最低——联想扩展丰富=信息多=噪音多，**同一特性两面：纯选奖励它，五维惩罚它**。

### 3.7 Graphiti 垫底归因（StoryRank 前后两环节拆解）

**候选层（StoryRank 前）是问题主因**：

| 指标 | PAR | Naive RAG | Graphiti |
|------|:---:|:---:|:---:|
| 平均候选数 | 38.3 | 38.3 | **33.6** |
| 达配额上限查询数 | 17/35 | 17/35 | **9/35** |
| 候选场景覆盖均值 | 7.6 | 8.4 | **6.8** |
| 场景覆盖 < RAG 的查询数 | — | — | **31/35** |

机制：**事实级检索的"集中效应"**（返回语义最近的事实及其图邻域，天然聚焦）+ **抽取-合并的信息收窄**（322 条原文收敛为 228 实体/268 边）+ 协议因素（故事喂原文 content 而非 Graphiti 事实文本）。**故事层（StoryRank 后）忠实反映输入、非缺陷**：故事长度 467 > 358 > 265、专有信息 5.2 > 3.2 > 2.2 均与纯选排位一致——StoryRank 是忠实的"输入整理器"，不放大也不补偿输入差异。另注意：PAR 的候选场景覆盖（7.6）低于 RAG（8.4）而候选数相同（38.3）——PAR 在 gold 图上联想沿场景内因果边组织、在更少场景内重复采样；这与 §4.8 的 DBA 图上发散（候选 877 次）是不同图组织下的两种现象，不矛盾。

**反证实验（StoryRank 不是削弱点）**：直接用 Graphiti 原始事实文本（`fact` 字段，均值 667 字/查询）与 PAR 对比——**PAR 28:2 胜率 0.933，输得更惨**。原始事实文本即使信息量更大（667 vs 265 字），在"组织连贯"维度被系统性扣分（判词反复出现"故事B仅为碎片化条目堆砌"）。纯选评测的是"作为组织化记忆的价值"，不是原始信息含量。

**故事字数 ≠ 故事质量**（4 轮共 120 次对照）：故事 vs 故事中更长者胜率高（83–90%，长度是丰富信息的副产品）；但"PAR 故事 vs 原始事实"中字数最长者输 90%——真正决定因素是**信息的组织与真实性**。对评测协议的含义：无需也不应做长度对齐。

### 3.8 概念定论（0821 定稿，详见 §5）

1. **在人工构建的纯情景记忆数据集的条件下，纯向量可能是记忆系统检索的最优默认方案**（0.840 > 0.765 > 0.642），图谱仅在联想检索维度有价值。措辞收敛为「可能」的论证：联想/发散价值难以度量——联想无对错之分，凡经跳转轴沿因果边发散的联想均有完整因果链路，无法按对错量化；关键词与图遍历在当前评测数据集上未起良好优化效果，强行叠加可能适得其反；三者+RRF 混合检索的部署成本亦为否决因素之一；
2. **Mem0 图谱功能不引入**（spaCy 英文启发式，中文下预期与 BM25 同一下场——推断，未实测；"图谱+向量+关键词"三查叠加牵强且成本失控）；
3. **知识库 ≠ 记忆系统**（脑科学视角，§5.2）；
4. 复杂度约束：Graphiti 混合检索（向量+BM25+图遍历三查叠加）在纯情景记忆数据上未带来任何检索层优势——**检索机制不是越多越好，应随场景取最适配的单一路径**。

---

## 四、DBA 抽取环节评测（0822）

### 4.1 评测设计

- **子集闭环**：LiFEMem 5 场景 100 节点（w/he/so/e/h 各 20）→ 还原为 15 批多轮口语化对话（含指代省略/寒暄噪声）→ 空图逐批 `MemoryDBA.maintain()` → 与 gold 子图（100 节点/138 边）对比。
- **全量闭环**：322 节点 → 51 批对话（`lifemem_full_dialogs.yaml`，322/322 覆盖）→ 批量 6 → 9 次维护。
- 指标：节点召回/精确/类型正确率；边召回/精确；R@all（子集内 expected）。

### 4.2 首版结果（改造前）：双重压缩

| 配置 | 节点召回 | 节点精确 | 类型正确 | DBA 边数 | 检索 R@all* |
|------|:---:|:---:|:---:|:---:|:---:|
| gold 子图 | — | — | — | 138 | **91.67%** |
| 逐轮（15 次维护） | 54% | 81.8% | 48% | 53 | 0.00% |
| 批量 6（3 次维护） | 49% | 86.0% | 65% | 38 | 20.00% |
| **DBA 节点 + gold 边（混合图）** | — | — | — | — | **48.00%** |

\* R@all 基于检索器**全量输出**（峰值容忍带内候选，平均 25–30 条，非固定 top-N）；数值为**重标口径**：为 DBA 图独立重标 expected（LLM 对 DBA 节点内容标注 2~4 个/条，不使用图结构），替换原 gold 反推 expected。原口径下逐轮/批量6/混合图分别为 0% / 8.33% / 41.67%。**本表为历史基准**（7 条有效查询/12 个 expected）单次运行；同基准（17 条/33 expected）重跑与 4 版本检索层鲁棒性见 §4.6。

**双重压缩机制**：gold 是人工"原子展开"标注，DBA 是 LLM 自动提纯（消解指代/补全省略/合并事实）产出的"压缩记忆"——批量 6 节点压缩 43%、边压缩 72%（逐轮 32%/62%）。压缩同时削减联想路径（边 138→38，PAR 检索沿边走不了几步）。**混合图实验（补 gold 边）R@all 由 20.00% 回升至 48.00%（2.4 倍）→ 边连接是检索跑偏的直接原因。**

> 口径声明：原 expected 系从 **gold 图**反推标注，使 gold 图在 R@all 对比中天然占优。**已修复**：为 DBA 图独立重标 expected（LLM 对 DBA 图全部节点内容标注、不使用边/邻接信息，粒度与 gold 对齐 2~4 个/条），本表及 §4.4/§4.5/§4.7 的 DBA R@all 均采用重标口径；原 gold 反推口径仅在 §4.8 跨图命中分析中保留。纯故事评测（无查询）不依赖 expected，是两图的公平对比（§4.5/§4.6）。注：重标为 LLM 单次判定，未多次取均值。

### 4.3 边压缩归因审计（A/B/C）

gold 138 条内部边分类：

| 类别 | 数量 | 占比 | 含义 |
|------|:---:|:---:|------|
| A 正确连接 | 4 | 2.9% | DBA 连对了 |
| B 两端都抽到但未连 | 37 | 26.8% | LLM 连边判断/可见性问题 |
| **C 一端节点缺失** | **97** | **70.3%** | **节点压缩直接导致无法连边** |

机制确认：改造前 node/edge ops 在同一次 LLM 输出中产生，LLM 连边只能引用「top-15 相关节点 + 一跳邻居 + 本轮新建」——**可见节点集合直接限制连边能力**。结论：**边压缩是节点压缩的下游果（70% 归因），LLM 独立保守连边仅占 27%。**

**B 类细分**：B1 保守未连 ~29 条（端点改写基本保留语义，LLM 没连）；B2 信息改变致断 ~8 条——最典型是**「习惯→单次」时间压缩**（gold「颈椎酸痛→每天晨跑半小时」被抽成「今早绕小区跑步半小时」，晨跑习惯变单次事件，因果链断）、多事实合并淹没关系对象、主体/场景收窄。

### 4.4 改造：两步拆分 + 激进连边（对抗压缩）

**两步拆分**（落地于 `dba_pipeline/extraction/dba.py`）：Step 1 节点抽取（强化"一个事实一个节点 + 保留时间维度"）；Step 2 边连接（可见本轮**全部**新节点，不再受 top-15 限制，可注入连边策略）。

**连边策略四版对比**（均批量 6）：

| 版本 | 节点 | 边 | 边召回 | 边精确 | 边类型一致 | R@all* |
|------|:---:|:---:|:---:|:---:|:---:|:---:|
| v1 合并式（基线） | 57 | 38 | 2.9% | 15.8% | 33% | 20.0% |
| v2 两步（无示例） | 99 | 126 | 26.1% | 34.9% | 52% | 22.2% |
| v3 两步+克制 few-shot | 98 | 118 | 23.2% | **37.3%** | 52% | 25.9% |
| **v4 两步+激进 few-shot** | 87 | **170** | **34.8%** | 35.9% | **77.1%** | 17.4% |

\* 重标口径（§4.2 口径声明）；原口径下四版为 8.3% / 25.0% / 8.3% / 25.0%。**本表 v1–v4 均为历史基准**（7 条/12 expected）单次值，R@all 列仅示波动范围、不支持版本间排序；v4 同基准重跑见 §4.6。

**激进连边（v4）定为当前策略**：「宁可多连」在 PAR 检索下安全（寻峰/目的过滤兜底），多连不伤检索；克制版连得过少、联想路径断裂。**注意：子集 R@all 在重标口径（历史基准）下四版差异（17.4%–25.9%）全部落入小样本噪声范围（7 条查询、23–27 个 expected），不再支持任何版本间的 R@all 排序**——v4 定案依据是边召回/边类型一致（34.8%/77.1% 最高）而非 R@all；原口径下 25%/8.3%/25% 的波动同样印证「子集 R@all 单次不可靠，可靠对比需多次取均值」。

**手写目的注入测试**（排除目的推断噪声）：注入更聚焦目的（s01→「倾诉工作疲惫」）后 DBA R@all 不变（原口径 25%）、工作查询依旧 0；gold 图反被手写目的拖累（91.67→66.67%）——**目的推断不是跑偏主因，检索对 purpose 表述高度敏感**。s01 类跑偏 = PAR 检索联想发散（目的驱动 + 图结构组织），与抽取缺失、目的推断均无关。

### 4.5 结构性结论：gold 图非最优，图结构一致性要求低（定论）

以较大结构差距（gold 138 边 vs DBA 170 边，**边重合率仅 34.8%**）评测两图实力：

| 评测范式 | gold | DBA | 结果 |
|------|:---:|:---:|------|
| 检索 R@all* | 81.82% | 29.09% | gold 大幅领先 |
| 带查询·五维盲评 | **4.06** | 3.54 | gold 胜（差距在相关性） |
| 纯故事·无查询四维 | 4.575 | **4.75** | **DBA 反超** |

\* v4 **同基准重跑**（17 条查询/33 expected，§4.6）；DBA 为重标口径（原映射口径 24.24%）。历史基准（7 条/12 expected）下 gold 91.67% / DBA 重标口径 17.39%。

> 注：纯故事·无查询四维 = **信息丰富度（richness）/ 真实性（authenticity）/ 连贯性（coherence）/ 记忆价值（memory_value）**，为无查询专属 rubric（产物见 `dba_story_alone*.json`），与带查询五维（§2.1/§3.5）不是子集关系——跨范式比较总分时需注意口径差异。

**结论**（限定在人工构建的纯情景记忆数据集条件）：gold 子图不是最优图，图结构一致性要求低——图无需与 gold 结构对齐（35% 重合）即可达到相当水平。要点：① 与 gold 的一致性不是敏感变量；② **完整性仍必要**——边不是"连对"而是"有边可走"（混合图实验补 gold 边后 R@all 由 20.0% 回升至 48.0%，§4.2）；但子集小样本内版本间 R@all 差异不显著（历史基准 v1 20.0% vs v4 17.4%，同基准重跑见 §4.6），边稀疏的检索损伤需在全量层面看；③ gold 优势仅在检索层聚焦，故事化后差异大幅收敛（带查询 0.52 → 纯故事反转）；④ **DBA 自动建图足以与人工 gold 图不相上下** → 建图优化应转向「保证连通完整」而非「追求与 gold 结构一致」；⑤ 支撑评测计划的反证：人工图信息保真度不高于 DBA 自动维护图（记忆价值维度成立）。

### 4.6 多版本鲁棒性验证（4 次独立抽取）

同一语料、同一激进连边 prompt 再抽取 v5/v6/v7（+v4 共 4 次独立 LLM 抽取）：

| 指标 | v4 | v5 | v6 | v7 | 波动 |
|------|:---:|:---:|:---:|:---:|:---:|
| 节点 | 87 | 88 | 85 | 85 | 3 |
| 边 | 170 | 198 | 174 | 208 | 38 |
| 节点召回 | 76.0% | 76.0% | 76.0% | 75.0% | **±1.0%** |
| 节点精确 | 87.4% | 86.4% | 89.4% | 88.2% | ±3.0% |
| 边召回 | 34.8% | 37.7% | 33.3% | 34.1% | **±4.4%** |
| 边类型一致 | 77.0% | 46.0% | 66.7% | 54.2% | ±31.1% |

**纯故事对比**：4 个版本 DBA 总分**全部恒为 4.725、全部高于 gold**（gold 4.600/4.700/4.600/4.675）。结论：① 纯故事表现恒定——DBA 反超是稳定结论非单次偶然；② 核心指标稳定（节点召回 ±1%、边召回 ±4.4%、精确率 ±3%）；③ **次要指标波动大但无关紧要**——类型正确率 57–70%、边类型一致率 46–77% 波动大，但纯故事完全不受影响（类型/边类型标注不是性能敏感项）；④ 结构差异大（边 170–208、覆盖边 Jaccard 0.61–0.72）而性能恒定——"图结构一致性要求低"实证闭环。

**检索层鲁棒性（补测）**：4 版本用当前脚本**同基准**重跑子集检索（17 条有效查询、33 个 gold expected——§4.2/§4.4 的子集数字为**历史基准**：7 条查询、12 个 expected），并对各自图重标 expected 后按重标口径计 R@all：

| 指标 | v4 | v5 | v6 | v7 | 波动（std） |
|------|:---:|:---:|:---:|:---:|:---:|
| DBA R@all（重标口径） | 29.1% | 58.6% | 37.7% | 50.8% | **±11.4pp** |
| DBA R@all（gold 映射口径） | 24.2% | 57.6% | 42.4% | 54.5% | ±12.4pp |
| gold R@all（同一 gold 子图重跑） | 81.8% | 90.9% | 87.9% | 87.9% | ±3.8pp |

**结论**：① **检索层波动远大于故事层**——纯故事恒定 4.725，而重标口径 R@all 范围 29.1%–58.6%（std ±11.4pp），「核心指标稳定」**不延伸至检索联动**（回应 §6.3 悬置项）；② **gold 本身亦不稳定**——同一 gold 子图 4 次重跑 R@all 在 81.8%–90.9% 波动（std ±3.8pp），PAR 检索含 LLM 目的推断，单次运行（即便 temperature=0）的 R@all 均为抽样值；③ **任何单次 R@all 数字不可作精确对比依据**（含本报告各表），可靠对比需多次取均值；但**方向性结论仍稳健**——gold 最低值（81.8%）高于 DBA 最高值（58.6%），两区间不重叠，gold ≫ DBA 的方向可靠、差距幅度不精确；④ v4 单次重跑偏低（29.1%）不构成对 v4 策略的否定——同基准下四版差异（29.1–58.6%）全部处于抽取随机性范围。

### 4.7 全量验证（322 节点闭环）

**全量抽取**（批量 6 → 9 次维护）：实质节点召回 **75.2%**（197/262，排除噪声后与子集 76% 一致）、精确 77%、边 529（新边 218）。**噪声过滤全量生效**：噪声批（d43–d51，60 节点）被 DBA triage 主动跳过（node_ops=0）。抽取水平在全量尺度稳定复现。

**全量检索联动**（35 条查询，PAR 检索）：**gold 77.78%（63/81）vs DBA 50.46%（55/109）**（DBA 侧为重标 expected 口径：30 条查询、109 个重标 expected 中命中 55；原 gold 反推口径为 30.86%。注意 DBA 分母 109 > gold 81——重标标注粒度略宽于 gold，50.46% 是保守下界）。分场景两图对比（DBA 为重标口径 hit/expected，gold 为 expected 口径）：

| 场景 | gold | DBA | 差距 |
|------|:---:|:---:|:---:|
| work_pressure | **100%**（11/11） | 66.7% | −33pp |
| learning | **100%**（9/9） | 63.6% | −36pp |
| **nostalgia** | **90.9%**（10/11） | **6.2%** | **−85pp** |
| life | **90.0%**（9/10） | 58.3% | −32pp |
| emotion | 70.0% | 69.2% | ≈持平 |
| health | **62.5%** | 41.7% | −21pp |
| **social** | 62.5% | **90.0%** | **+28pp** |
| **future** | 50.0% | **75.0%** | **+25pp** |
| relationship | **50.0%** | 8.3% | −42pp |

关键：**nostalgia 不是两图都差而是 DBA 独差**（gold 90.9% vs DBA 6.2%）——学生时代漏检是 DBA 图联想路径组织缺失（gold 图在该场景内部连接完好），而非查询集固有困难；同时 **DBA 在 social/future 场景检索反超 gold**（+28/+25pp）——DBA 自动建图在部分场景的组织优于人工 gold 图，检索层亦非全面落后。

**命中差异不对称**（跨图视角，基于 gold expected 映射口径）：命中集合完全一致仅 3 条；G 特有 43 节点（25 条）vs D 特有仅 5 节点（5 条）——gold 命中集基本是 DBA 父集。G 特有中**学生时代系（h 7 + ln 9 + c 7）占 53%**；该漏检**非抽取缺失**（s14/s15/s17/s18 的 expected 抽取全命中），是 PAR 检索在 DBA 图上沿边回不到学生时代节点（gold 图有 h/ln/c 场景内部密集连接，DBA 图欠组织）——重标口径下 s13–s17（怀旧/感情）依然 0 命中，同一结论得到重标口径印证。

**全量 StoryRank**：

| 评测范式 | gold | DBA | 结果 |
|------|:---:|:---:|------|
| 检索 R@all* | **77.78%** | 50.46% | gold 领先（差距 27.3pp） |
| 带查询·五维盲评 | 3.726 | **3.674** | **差距仅 0.05** |
| 纯故事·无查询四维 | 4.586 | **4.643** | **DBA 反超**（四维全优） |

\* DBA 侧为重标 expected 口径（§4.2 口径声明）；原 gold 反推口径 30.86%，重标后大幅回升（差 46.9pp → 27.3pp）——原口径系统性低估 DBA 检索。数据越充分，StoryRank 弥合作用越强（子集差 0.52 → 全量差 0.05）；纯故事 DBA 稳定反超（子集 + 全量一致）。

### 4.8 检索的「共识区与发散区」（理论框架）

> 本节的命中分析基于 **gold expected 映射口径**（跨图视角：DBA 候选映射到 gold id 后比对），与 §4.2/§4.4/§4.5/§4.7 的重标口径 headline 不同——它回答的是「DBA 图覆盖了多少 gold 定义的答案」，而非「DBA 覆盖多少自身 expected」。

| 分区 | 定义 | 节点数 | 性质 |
|------|------|:---:|------|
| **共识区** | 两图共同命中的 expected | **17** | 查询命题内语义相关公共答案，与图结构无关 |
| **发散区** | 两图各自独有命中 + 非 expected 候选 | G 特有 34 / D 特有 5 | 因图结构不同而不同 |

DBA 候选构成（30 条有效查询，**877 次候选**）：命中 expected 25 次、发散未命中 **642 次（去重 166 节点）**、完全未映射 210 次（占 24%——DBA 候选节点在 gold 图中无对应节点、无法按 content/id 对齐，反映两图节点构成差异）。**共识区（17 个两图共同命中节点）是 DBA 命中的主体（25 命中中 17 来自共识区）**——查询命题内答案 DBA 基本没漏；gold 的 R@all 优势完全来自发散区命中（场景内密集组织让发散恰好扫中更多 expected）。**发散是检索输出的绝对主体**（642 vs 25 命中）——纯故事评测度量的正是发散区的记忆价值。

**理论收口**：带查询评测测「共识区 + 发散命中运气」（语义收敛），纯故事评测测「发散区记忆价值」（联想丰富）——两范式分别度量检索的两个属性，两图恰各胜其一，这是「评测范式决定胜负」的机制根源。

### 4.9 第二轮边连接实验（遍历联想连边）

- **对话驱动二次连边 = 零变化**：无新节点即无连边动机，9 轮全部 create=0/delete=0——边连接器是「对话驱动的增量连边」，不会自发补全旧节点关系；
- **遍历联想连边三档**（按节点 content 语义分类分批，从原始 529 边）：保守 +109（效率 5.2:1 最优）、中间 +239（性价比最低）、激进 +965（边召回 52.2% 最高、精确腰斩 13.2%）；收益曲线**先快后慢**；
- **补连对检索收益极小（10 条预览样本，证据强度有限）**：原始 48.1%、保守 48.2%（零增益）、激进 51.9%（+3.7pp，含噪声）——在该小样本内未见边覆盖提升带来检索提升，PAR 检索似对联想路径组织敏感而非边数量；该否定性结论需扩大样本后确认。

### 4.10 收官定论：联想链路的前置性

在人工构建的纯情景记忆数据集条件下，整个评测链收敛为一个完整架构：

```
抽取层（联想源头）          检索层（利用联想）          评价层（度量选择）
  LLM 自发连边  ──边结构──▶  共识区（纯向量下限）  ──▶  带查询：语义收敛
  + 人工干预（YAML/CRUD）         +                    纯故事：联想丰富
  （决定检索上限）          发散区（图结构增量）
```

1. **在本数据集条件下，联想从前置抽取开始**：PAR 检索的联想/发散行为主要由图的边连接决定（补连实验保守 0 增益、激进 +3.7pp，基于 10 条预览、旧口径的初步证据）——检索只是**利用**边结构，不创造联想；联想的"原料"在抽取层就定了；
2. **检索上限受制于数据集的边连接**：边是检索瓶颈的直接证据来自混合图实验（补 gold 边后子集 R@all 20.0%→48.0%，§4.2）、边密集则发散充分（激进补连至全图 **1616 边**后，平均候选 28.6→70.1 近翻倍，见 §4.9 产物 dba_relink_all / dba_retrieval_relink_agg）；gold 的场景内密集组织正是其全量 R@all 领先（77.78% vs 50.46%）的来源；
3. **边连接 = LLM 自发 + 用户人工干预**：LLM 增量连边为主、不会自发补全；**YAML 单文件让用户与 LLM 都能直接读写图谱**，构成双通道干预载体（人工编辑 + DBA/MCP 自动维护）；
4. **共识区 = 纯向量语义下限**（embedding 基本盘，图结构无关），**发散区 = 图结构增量**（联想上限）；
5. **评价范式 = 度量切面选择**：带查询测共识区+发散命中、纯故事测发散区记忆价值——联想在抽取层分叉是「评测范式决定胜负」的根源。

---

## 五、概念定论与理论升华

### 5.1 记忆系统检索技术的定位（0821 定稿）

1. **在人工构建的纯情景记忆数据集的条件下，纯向量可能是记忆系统检索的最优默认方案**（相对图谱与关键词，除联想检索外）：检索层 expected 覆盖 RAG/Mem0（纯向量）0.840 > PAR（图谱）0.765 > Graphiti（图谱）0.642；BM25 中文失效、英文净负效；**默认走纯向量，图谱只作联想扩展的可选增强，不作检索主干**。措辞收敛为「可能」的论证：联想/发散价值难以度量（联想无对错之分，凡经跳转轴沿因果边发散即有完整因果链路）；纯向量的直接证据链是纯故事对比中完胜 Graphiti（§3.6，26:4）；关键词与图遍历在当前数据集上未起良好优化效果，强行叠加可能适得其反；三者+RRF 混合检索的部署成本亦为否决因素之一；
2. **综合两范式，RAG 最均衡；图谱（Graphiti）记忆保真垫底**：Graphiti 落后根因已定位（事实级检索集中效应 + 抽取合并信息收窄；反证排除故事化责任）；Graphiti 虽在带查询五维唯一领先（连贯/噪音双第一，4.74 > RAG 4.67 > PAR 4.51），但纯选垫底（26:4 / 23:6），综合两范式 RAG 最均衡——**图谱精炼表达对"查询贴合"友好，对"记忆保真"不利**；
3. **Mem0 图谱功能定论：不引入**：实体 boost 依赖 spaCy 英文启发式，中文下预期与 BM25 同一下场（推断，未实测）；"图谱+向量+关键词"三查叠加过于牵强且成本失控；
4. **复杂度约束**：混合检索（向量+BM25+图遍历）在纯情景记忆数据上未带来任何检索层优势——**检索机制不是越多越好，应随场景取最适配的单一路径**。

### 5.2 知识库 ≠ 记忆系统（脑科学视角）

知识库与记忆系统同属外部上下文工程，但机制与意图不同：

| 维度 | 记忆系统（本项目） | 知识库（RAG 类） |
|------|------|------|
| 脑科学对应 | **情景记忆（episodic）** | **语义记忆（semantic）** |
| 核心脑区 | 海马体（编码/提取个人经历与情景） | 颞叶新皮层（存储通用概念与事实） |
| 数据性质 | 个人经历、时序、情感绑定、高冗余 | 通用知识、静态、去个人化 |
| 检索意图 | 回忆具体经历（联想/时序敏感） | 查询事实概念（精确/稳定） |
| 更新机制 | 增量写入 + 遗忘/冲突处理 | 知识更新与校验 |

**后续方向**：agent 记忆系统（情景记忆）与知识库（语义记忆）的**真正分离与合作**——各自采用最适配检索机制（记忆=向量+情景联想，知识=向量+精确召回），明确接口（何时查记忆/何时查知识/如何融合），**而非在同一系统里堆叠所有检索方法**。本轮"图谱三查叠加无收益"的实证恰好支持"分离而非堆叠"。

---

## 六、总结论

### 6.1 各环节结论汇总

| 环节 | 结论 |
|------|------|
| 评测范式 | 带查询评测系统性低估联想型记忆系统；**评测范式决定胜负**（问答 RAG 胜 / 记忆框架 PAR 胜 83%） |
| 检索层 | 纯向量可能最优（0.840，论证见 §5.1）；BM25 中文失效/英文净负效；图谱仅联想维度有价值 |
| 主指标 | T6 五维 Graphiti 4.74 > RAG 4.67 > PAR 4.51；纯故事 PAR 全面占优（26:4/25:5/23:6） |
| DBA 抽取 | 双重压缩（节点/边）→ 两步拆分 + 激进连边修复；节点召回 75–76%、边召回 35%、边类型一致 77% |
| 图结构 | gold 图非最优；结构一致性要求低（35% 重合打平，4 次独立抽取纯故事恒定 4.725 反超 gold） |
| 联想链路 | 联想源头在抽取层；共识区=纯向量下限、发散区=图结构增量；YAML 双通道干预 |

> 注：上表各环节结论均限定在**人工构建的纯情景记忆数据集的条件下**，外推至真实规模、非人工标注的记忆数据需谨慎。

### 6.2 与理论部分的闭环

- **下游依赖成立**：前置数据管理产出的图谱质量直接决定检索阶段上限——DBA 全量图 50.46% vs gold 77.78% 的检索差距（重标口径）恰由"场景内密集组织（边结构）"缺失造成（**nostalgia DBA 6.2% vs gold 90.9% 的独差最典型**，其余差距 21–42pp），而带查询 StoryRank 差距仅 0.05、纯故事反超，印证"完整性必要、结构一致不必要"；
- **上游反馈成立**："只连有联想价值的边"与"边衰减遗忘"的设计在评测中得到验证——激进连边在 PAR 检索下安全（寻峰/目的过滤兜底）、噪声图不主动连边（triage 过滤）；
- **方法论立场延续**：检索层收束支持"不堆叠检索方法"；DBA 自动建图不劣于人工 gold 图，支撑"将最难部分外包给 LLM"的设计哲学。

### 6.3 局限

1. 35 条查询样本有限，分情境后每类仅 2–4 条，小样本波动；
2. judge 为 DeepSeek 非 GPT-4o，不可与公开榜单直接比；纯故事对比为单模型单次判定，未重复取均值（DBA 版本鲁棒性已用 4 次独立抽取补偿）；
3. expected 依赖人工标注；"长度与胜率正相关"未完全排除 confounder（长度可能被 judge 当"更详细"的 proxy，但反证实验显示组织度才是决定因素）；
4. PAR 检索在 DBA 全量图上的学生时代漏检（nostalgia 6.2% vs gold 90.9%、relationship 8.3% vs gold 50.0%——**DBA 独差而非两图共弱**）提示 DBA 图的学生时代联想路径组织仍是检索侧待改进点；
5. DBA 全量检索评测基于单次抽取，LLM 抽取随机性意味着全量数值存在单次波动（子集 4 版本重跑显示单次 R@all 波动 std ±11.4pp，§4.6——全量单次值波动幅度类似，但全量 DBA 50.46% 与 gold 77.78% 的差距方向在子集波动范围内仍稳健）；
6. **expected 口径**：原 gold 反推 expected 使 gold 图在 R@all 对比中占优；已为 DBA 图独立重标 expected（§4.2 口径声明）修复，但重标为 **LLM 单次判定**（temperature=0，未多次取均值），且重标后 DBA 侧与 gold 侧的 expected 来源不同（LLM 标注 vs 人工标注），两口径数字并列呈现时需注意不可直接混用；纯故事对比不受影响；
7. **被试与裁判同源**：DBA 抽取、StoryRank 故事化、judge 盲评、PAR 目的推断均基于 api.gpt.ge 的 deepseek 系模型，模型自身偏好未被独立模型交叉验证；
8. **数据与查询按 PAR 范式构造**：9 条时序演进线（保证因果链走通）、场景内密集组织、陈述触发式查询集均按 PAR 设计，对图谱/联想类系统存在构造偏置，外推受限；
9. **噪声过滤评测未兑现**：60 个噪声节点仅经 DBA triage 侧证（噪声批 node_ops=0），检索/故事层未系统测噪声节点进候选率；
10. **检索层波动大**：已补测（§4.6）——4 版本同基准重跑的重标口径 R@all 波动 std ±11.4pp（29.1–58.6%），同一 gold 子图重跑亦波动 ±3.8pp（81.8–90.9%），**单次 R@all 不可靠**；补连结论基于 10 条预览样本。

### 6.4 后续方向

- **建图侧**：转向「保证连通完整」而非「追求与 gold 结构一致」；关注「习惯 vs 单次」时间维度保留（B2 类断裂）；
- **评测口径**：gold 原子展开 vs DBA 压缩记忆，匹配改「语义覆盖」（LLM 判定）替代一对一余弦；
- **检索侧**：knowledge_update 时点对比、联想向后回溯两大待补机制；候选排序层精度截断；分情境补足样本（每类 ≥8 条）；多次重复判定取均值；
- **架构侧**：知识库与记忆系统分离合作（§5.2）——明确"何时查记忆/何时查知识/如何融合"的接口。

---

## 七、0827 时序功能专项：TEMPORAL 缺失诊断与设计收敛

> 本节为 2026-08-27 追加的时序专项，超出核心报告的 0818–0822 范围；记录系统对 `TEMPORAL`（事件→时间，**单向**）关系的系统性缺失诊断、专项实验与设计收敛。精简版见 [TEMPORAL 时序功能实现总结报告](Plan/TEMPORAL时序功能实现总结报告.md)，完整过程/实验/决策见 [0827 时序关系专项实验与链路补全](Plan/0827——TEMPORAL时序关系专项实验与链路补全.md)。

### 7.1 问题定位：TEMPORAL 为何被系统性避开

理论部分将 TEMPORAL 定义为**单向关系（事件→时间）**（§3.2 跳转轴中反向统一阻断）。但真实链路三环相扣地把它忽略了：

| 层 | 现状 | 证据 |
|----|------|------|
| 数据集 | 全量 TEMPORAL 边仅 8 条（~2%），全为年代级粗锚点（`h*→th0` 高中、`c*→th1` 大专、`w*→th2` 工作），锚点标为 `thing`；子集（100 节点）不含 th 时间节点 → temporal=0 | `data/LiFEMem.yaml` |
| 抽取 | 两步拆分后边连接 few-shot **无 TEMPORAL 示例**；节点抽取 prompt **无"时间节点"引导**；节点类型无 TIME | `dba.py` |
| 检索 | TEMPORAL 前向 0.3–0.5、**反向恒为 0**；时间锚点 thing 仅 0.3 → PAR 检索沿 temporal 边几乎不扩展 | `jump_axis.py` |

**结构性根因**：TEMPORAL 需要"时间节点"作为对象，但建图时无引导、无示例 → 抽取层从不产出；即便产出，检索层也因极低权重而忽略。三环相扣。

### 7.2 受控实验：TEMPORAL 是"时间线可重建"的充要条件

**实验一（17 节点事件级时间线，端到端闭环）**——同一语料对比 base 与"时间节点引导 + TEMPORAL few-shot"（[run_temporal_experiment.py](eval/run_temporal_experiment.py)）：

| 配置 | 节点 | 边 | 节点召回 | **temporal 边** | 可锚定率 | 时间线一致率 |
|------|:--:|:--:|:--:|:--:|:--:|:--:|
| gold | 17 | 21 | — | 10/10（100%） | 100% | 1.0 |
| base | 16 | 39 | 52.9% | **0/10（0%）** | **0%** | N/A（无法建时序） |
| temporal | 18 | 27 | **76.5%** | **4/10（40%）** | 43–53% | 1.0 |

- base 零 temporal 边：LLM 把时间"焊进"事件内容（如"项目于周三凌晨上线"），无独立时间节点，无法连 TEMPORAL；虽事件召回 84% 高于 temporal，但**可锚定率 0%**、时间线重建 N/A——没有 TEMPORAL 锚点，召回再多也是一堆无序事实。
- 加引导后节点召回 53%→77%、temporal 召回 0→40%，时间线一致率全 1.0。**缺口可修复，只需引导 + 示例。**

**实验二（LiFEMem 全量，补 91 条 TEMPORAL 边）**（[run_temporal_lifemem.py](eval/run_temporal_lifemem.py)）：按人生阶段前缀补充 `h*→th0 / c*→th1 / w*→th2`，锚定事件 8→99：

| 图 | R@all | 可锚定率 | 跨纪元 pairwise | 期内(SEQUENCE) | 期内(SEQUENCE+CAUSAL) |
|------|:--:|:--:|:--:|:--:|:--:|
| 原始（仅 8 条） | 77.0% | **56.9%** | 1.0 | 0.50 | 0.50 |
| **补充版（91 条）** | **96.0%** | **100%** | **1.0** | 0.66 | **0.88** |

- TEMPORAL 补边把"高中<大专<工作"的**纪元骨架**建对（跨纪元 pairwise 恒 1.0、可锚定率 32%→100%），R@all 回升；期内精细时序单靠 SEQUENCE 不够（0.66），SEQUENCE+CAUSAL 共同定序升至 0.88。

### 7.3 三根因诊断（RC）

| 编号 | 根因 | 证据 |
|---|---|---|
| **RC1** | 抽取层是硬瓶颈：`R@all` 与「可锚定率」解耦 | 基线抽取 temporal 边=0 → `anchorable=0`；引导到 61.5% 但过度拆分（节点 23→38、边 43→66、边精确 23.3%→21.2%） |
| **RC2** | 检索层跨期（TD）是真弱势 | gold 图（数据完美）TD R@all 仅 0.17 → boost 0.67 → dba_base 0.83；"多 th 齐聚"是真实检索困难 |
| **RC3** | 「时间→事件」是词面假象 | gold 图默认跳转（反向权重=0、无 th→事件遍历）下 TB R@all 竟 1.0，因事件 content 字面含时间词被向量直接命中；**纯图结构的时间→事件反向从未实现** |

### 7.4 设计收敛：独立单跳时序工具，主检索零污染

- **全局化不可行**：改默认 prompt / 全表提权虽让故事主指标真实变好（5 维含「时序结构」后 boost 全面超噪声底，structure +0.30），但在常规语料造成抽取过度拆分（节点精确 87%→61%）、检索 R@all 约 −4pp、CROSS_R 变差 → 两处代码已回退。
- **手术式检索 = 空操作**："定向启用"TEMPORAL（`_is_temporal_query`/真锚点豁免/定向权重）经增量复测证实 ON/OFF 路径一致、structure 无差 → 废弃。
- **收敛定论（系统是「模拟联想、无离线经历」）**：时序是**符号查找/排序原语**，不是时间记忆体。落地为：主检索器保持纯基线 + 独立单跳 `temporal_lookup` 工具（仅「时间→事件」反向、单跳、多锚点 ≤3、真锚点校验 `_has_reverse_temporal`）+ MCP 工具 `dba_temporal_lookup` + LLM 路由分流 + 抽取侧手术式守卫 `has_temporal_signal`（时序叙事才启用 TEMPORAL）。

### 7.5 验证（全绿）

- **功能/端到端/路由/稳定性**：`test_temporal_lookup.py`、`test_temporal_full.py`、`test_tool_dispatch.py`（LLM 7/7 正确分流）、`test_temporal_stability.py`（18 项 PASS）、`tests/test_dba_temporal.py`（11 项 PASS）、闭环与增量 `test_temporal_closedloop.py`、去词面化确证 `verify_temporal_delex.py`、动态更新 `test_temporal_dynamic.py`、MCP 链路 `test_mcp_link.py`。
- **关键样例**：`temporal_lookup("昨天用户都做了些什么")` → 锚点 `T10(昨天)` → `{e9,e10,e12}`（+ 候选 `T11 今天`）；`用户当前工作时期怎么样` → `th2` → `{w0,w13}`；跨期/历程（从高中到工作）正确地归主检索器。
- **零回归确证**：抽取侧守卫下 0822 子集 node_precision 91.1%、0 条 TEMPORAL 边、未过度拆分；去词面化数据集确证「时间→事件」反向遍历**靠 TEMPORAL 图结构**，非词面。

### 7.6 局限与待决

- `retrieve()` 是**会话态**（`PathTracker.lifetime_counts` 跨调用累积，终身增强，刻意设计），同实例多跑非 bit 确定，但关键节点稳定、无回归；
- 待决：正式 TIME 节点类型（根除内容正则误判，但改动面 6→7 类/prompt/权重/校验）；细粒度锚点来源（322 图仅阶段级 th0/th1/th2，「昨天/上周」无真实节点，需抽取侧引导）；现实/虚拟 present 基准指针；锚点语义定位质量（"三年"→大专/工作歧义已用多锚点缓解，非根治）。

---

## 附录：报告与产物索引

| 日期 | 报告 | 关键产物 |
|------|------|----------|
| 0818 | [自建个人记忆数据集评测计划](Plan/0818——自建个人记忆数据集评测计划.md) | 数据构建协议、查询集 7 类、公平性五同 |
| 0818 | [LiFEMem 全量实验报告](Plan/0818——LiFEMem全量实验报告.md) | q01–q35 逐查询详情、分能力分析 |
| 0818 | [LiFEMem v3 全量数据报告](Plan/0818——LiFEMem_v3全量数据报告.md) | 9 类情境逐条数据、弃权类分析 |
| 0818 | [LiFEMem 实验总结与发现](Plan/0818——LiFEMem实验总结与发现.md) | 四轮范式修正、三框架评测、方法论闭环六点 |
| 0818 | [PAR 最差查询专项分析](Plan/0818——P最差查询专项分析.md) | q17/q27/q18/q19 逐条诊断 |
| 0819 | [Mem0 基准对比接入报告](Plan/0819——Mem0基准对比接入报告.md) | BM25 中文失效实证、英文对照实验 |
| 0821 | [Graphiti 基准对比接入报告](Plan/0821——Graphiti基准对比接入报告.md) | Neo4j 接入、三系统 T6、纯故事反转、概念定论 |
| 0822 | [DBA 抽取环节评测报告](Plan/0822——DBA抽取环节评测报告.md) | 双重压缩、两步拆分、全量验证、共识区/发散区、联想前置性 |
| 0827 | [TEMPORAL 时序关系专项实验与链路补全](Plan/0827——TEMPORAL时序关系专项实验与链路补全.md) | 时序缺失诊断、受控实验、RC1/RC2/RC3、正确补全路径、temporal_lookup |
| 0827 | [TEMPORAL 时序功能实现总结报告](Plan/TEMPORAL时序功能实现总结报告.md) | 精简总结：架构落地、三层测试全绿、遗留待决 |

数据与产物：`data/LiFEMem.yaml`（322 节点/410 边）、`data/LiFEMem_queries_v3.yaml`（35 条）、`data/dba_eval/lifemem_subset_dialogs.yaml`（100 节点→15 批）、`data/dba_eval/lifemem_full_dialogs.yaml`（322 节点→51 批）、`data/dba_graph_full.yaml`（DBA 全量图 529 边）；评测产物 `eval/outputs/` 下 p_candidates_v3 / naive_rag_candidates_v3 / mem0_candidates / graphiti_candidates / story_scores_v3 / story_scores_graphiti / story_texts_v3 / story_texts_graphiti / story_pair_{v3,pg,rg,p_facts} / story_compare_3sys / dba_extraction / dba_graph / dba_retrieval / dba_story / dba_relink 系列。0827 时序专项新增：`data/dba_eval/lifemem_temporal*.yaml`、`data/dba_eval/lifemem_crossperiod*.yaml`（含 `_queries`/`_dialogs`/`_delex` 变体）、`data/dba_eval/lifemem_full_temporal_augmented.yaml`；`eval/outputs/` 下 temporal_rooted / temporal_experiment / lifemem_temporal_cmp / story_alone_inj_temporal / dba_extraction_subset_guard 系列。
