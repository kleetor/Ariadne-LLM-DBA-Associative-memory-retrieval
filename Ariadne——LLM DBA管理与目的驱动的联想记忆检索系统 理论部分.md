# Ariadne—— LLM DBA管理与目的驱动的联想记忆检索系统 理论部分

>
> 本文由《LLM DBA 自动化记忆图谱构建》与《目的驱动的联想记忆检索模型》合并而成，构成完整的「对话日志 → 记忆图谱 → 联想检索 → 精排回复」理论闭环。

---

## 摘要

对话系统的长期记忆管理可以分解为两个相互依赖的子问题：**前置数据管理**——如何将非结构化的对话日志转化为结构化的记忆图谱；**在线检索**——如何从图谱中召回与当前查询相关的记忆。二者共同面临的困难在于：记忆的领域开放性、长期可维护性、以及"记忆之间的关联强度不等于语义相似度或图结构距离"这一隐含假设的失效。

本文提出 **Ariadne** 系统，将两个子问题统一为一个理论闭环，包含两个核心机制体系。

**第一，LLM DBA 前置数据管理。** 将记忆图谱视为一个由 LLM 自主维护的数据库——LLM 不是"一次性抽取器"，而是这个数据库的数据库管理员（DBA）。每次对话后异步执行一次维护操作，LLM 接收三层上下文（对话原文、语义相关记忆、图结构邻域），自主输出 CRUD 操作指令（create/update/fix_type/deprecate 节点，create/delete 边）。核心设计原则是"不求一次抽对，求持续纠错"：LLM 可以在后续轮次中通过 `fix_type` 修正类型、通过 `delete` 删除错误连线、通过 `deprecate` 标记过时记忆。系统由调度器（缓冲累积 + 五层触发）、DBA（上下文组装 + LLM 调用 + 指令解析）、GraphBuilder（操作执行 + 去重 + 校验 + 事务保护）三模块构成，并配以"边衰减而非时间衰减"的遗忘机制与"城市-高速公路"的图谱自组织模型。

**第二，目的驱动的联想记忆检索。** 将检索从"语义匹配"重新定义为"有向的联想搜索"，由三个机制构成：**跳转轴**（将 8 种关系类型化为 6×8 的有向扩展权重矩阵，提供方向性控制）、**目的回归**（从查询推断隐含目的并编码为目的向量，在每步扩展中过滤与目的无关的记忆）、**峰值容忍带终止**（以斜率变化定位峰值，在峰值附近容忍带内截取多轮候选，替代固定 top-K）。消融实验揭示三组件的维度分化——跳转轴主导召回深度（A2 无跳转轴在大图上退化至纯向量水平），目的回归的子机制分工明确（过滤控制召回-输出量权衡，加权提供精度微调）。

在人工构造的 47/63/83/216 节点多场景数据集上，系统完成两部分共计二十余项集成验证。前置数据管理侧：三轮增量模拟验证了建图→连边→矛盾重建的完整生命周期；DBA 自动构建的图谱在 P 检索 R@all 上与人工标注 GT 图持平（100%）；调度器行为测试 15/15 断言通过；M=6 确定为生产默认批量；P1/P2 优化将非 LLM 部分耗时压缩至 0.7s，使 LLM 推理成为唯一瓶颈（占 93%）。检索侧：跳转轴是召回深度的关键条件，P 相对纯向量检索 A 的 R@all 优势随规模单调放大（2.1x→2.8x→3.7x→3.4x）；系统性 CROSS_VALID 标注确认 P 的跨域合理联想为 A 的 6.4 倍（199 vs 31 条）；LESS 维度显示 P 平均排除 A 语义误命中的 38%，其中 TAXONOMIC 阻断将开放后的跨域干扰从 44% 降至零。

系统的核心设计启示是：**将最难的部分外包给 LLM，随 LLM 能力提升而零成本增益**——事实抽取、类型标注、矛盾检测、指代消解等全部随模型进步自然改善；而 FAISS 检索、图遍历、调度逻辑等固定开销已优化到可忽略水平。同时，检索的核心启示是：在长期记忆检索中，知道"往哪个方向找"和"为什么找"比"找得多近"更重要。需要指出，本文所有实验均在人工构造、精确标注节点类型与关系方向的数据集上完成，真实对话日志中自动抽取图结构条件下的泛化性验证是首要后续方向。

---

## 引言：系统总览

### 1. 全链路问题定义

对话系统的长期记忆管理可以分解为两个子问题，它们构成一个顺序依赖的管道：

```
对话日志 ──→ DBA维护 ──→ MemoryGraph + VectorStore ──→ P检索 ──→ StoryRank ──→ 回复
   │            │                    │
   │     MaintenanceScheduler       ├──→ API Server (HTTP REST + 3D 面板)
   │     (批量异步调度)              ├──→ MCP Server (6 tools, stdio/SSE)
   │                                └──→ 离线 HTML
   └──→ 人工干预 (CRUD面板 + MCP dba_intervene)
```

1. **前置数据管理**：将非结构化的对话日志（含角色、时间戳）转化为结构化的 MemoryGraph（节点 + 类型化有向边）。

```
输入：非结构化对话日志（含角色、时间戳）
    user(10:00): 最近压力太大了，天天加班到十点
    assistant: 听起来很辛苦，有放松方式吗？
    user(10:05): 喜欢喝咖啡提提神，最爱拿铁

输出：结构化的 MemoryGraph
    n1(加班) --CAUSAL--> n2(压力大)
    n3(喝咖啡) --PREFERENCE--> n4(拿铁)
```

2. **在线检索**：从图谱中召回与当前查询相关的记忆。传统语义向量检索与知识图谱扩展共享一个隐含假设——记忆关联强度等于语义相似度或图结构距离。这一假设的失效场景是：

```
用户说："最近压力太大了"

向量检索 → 匹配到"用户喜欢喝咖啡"（语义中等相关）
图谱扩展 → 从"喝咖啡"展开 → "拿铁" → "意式浓缩" → "意大利" → "意大利咖啡文化"

结果：检索到了"意大利咖啡文化"，但用户需要的不是这个。
```

关键缺失在于**方向感**：人类联想中"压力大"沿因果轴走到"加班"，再沿行为轴走到"喝咖啡"，每一步都有明确方向，整条联想链指向一个隐含目的（"找到缓解压力的方式"）。

### 2. 本文结构

本文由两部分构成，分别对应管道的两个子问题：

- **第一部分（前置数据管理）**：提出 LLM DBA 架构，将记忆图谱视为 LLM 自主维护的数据库，解决"如何持续把对话变成结构化、可纠错、可遗忘的图谱"。
- **第二部分（检索理论）**：提出目的驱动的联想记忆检索模型，由跳转轴、目的回归、峰值容忍带三机制构成，解决"如何有方向、不偏离、恰到好处地从图谱召回记忆"。

两部分的衔接关系是：**前置数据管理产出的图谱质量决定检索阶段的上限**——节点类型与边方向的标注精度直接决定跳转轴的方向性信号，`fix_type` 与 `delete` 既是维护功能，也是检索质量的持续保障机制。反之，检索阶段的四个核心发现（MORE/CROSS/LESS/CROSS% 随规模下降）反过来验证了前置数据管理中"只连有联想价值的边"与"边衰减遗忘"的设计正确性。

---

# 第一部分：前置数据管理——LLM DBA 自动化记忆图谱构建

## 一、引言

### 1.1 问题定义

对话系统的长期记忆管理可以分解为两个子问题：(1) **前置数据管理**——如何将非结构化的对话日志转化为结构化的记忆图谱；(2) **在线检索**——如何从图谱中召回与当前查询相关的记忆。本部分聚焦第一个子问题，其输入与输出见上文"全链路问题定义"中的示例。

### 1.2 核心挑战

传统知识图谱构建依赖管道式 NLP（NER → 实体消歧 → 关系抽取 → 图谱融合），在记忆系统场景中面临三个根本困难：

1. **领域开放性**：用户记忆涵盖工作、健康、社交、学习、情绪等所有生活领域，无法预设本体论或训练专用抽取模型。
2. **长期可维护性**：记忆是持续累积的——新事实会覆盖旧事实（"不喝咖啡了"），旧关系可能被修正（"之前以为是因果，其实是时序"），图谱需要支持完整的 CRUD 操作而非仅支持追加。
3. **指代消解与省略补全**：对话中充满指代（"那家店"→"公司楼下那家咖啡馆"）、省略（"喝咖啡"省略了"为了提神"）、第一人称（"我"→"用户"），需要上下文级别的语言理解才能将碎片化表述转化为独立可读的事实陈述。

### 1.3 为什么 LLM 比管道式 NLP 更适合

2026 年的 LLM 在对话理解、指代消解、省略补全方面已经过大规模预训练。将整个抽取-分类-连边流程交给一个 LLM 调用的优势：

| 能力 | 管道式 NLP | LLM DBA |
|------|:---:|:---:|
| 指代消解 | 需要专用共指模型 | 天然理解上下文 |
| 省略补全 | 无法处理 | 推断隐含信息 |
| 类型判定 | 需训练分类器 | 理解语义边界 |
| 纠错 | 无法自我修正 | fix_type / delete |
| 矛盾检测 | 需规则引擎 | 对比已有节点 |
| 跨批去重 | 需外部去重系统 | 向量语义去重 |

核心理念：**不求一次抽对，求持续纠错。** LLM 最擅长联想和纠错——让它们发挥这两个能力，而不是把它们限制在一次性的结构化输出里。传统管道式 NLP 的每个组件都是技术债——当 LLM 能力提升时，这些组件不会自动获益。而 LLM-native 架构则相反：LLM 的每一次进步，都会零成本地提升整个抽取-维护链路的质量。

### 1.4 本部分贡献

1. **记忆数据库模型**：将记忆图谱类比为关系型数据库——节点是主键记录，边是外键索引，LLM 担任 DBA 角色执行 CRUD 操作。
2. **三层上下文 + 维护判断机制**：LLM 接收对话原文、语义相关记忆和图结构邻域三层信息，自主判断本轮是否需要维护（避免对寒暄的无效调用）。
3. **完整的操作指令集**：4 种节点操作（create/update/fix_type/deprecate）+ 2 种边操作（create/delete），支持增量纠错和矛盾处理。
4. **异步批量调度器**：五层触发策略（被动/主动/自动跳过/空闲/会话结束），M=6 批量累积优化。
5. **遗忘机制**：边衰减替代时间衰减，LLM 的"不作为"自然驱动冷门记忆淡出。
6. **9 项集成验证**：覆盖建图、增量维护、矛盾处理、大图性能、检索联动、调度器行为、M 最优批量。

## 二、相关工作

### 2.1 知识图谱构建

传统知识图谱构建采用管道式架构：命名实体识别（NER）→ 实体链接（Entity Linking）→ 关系抽取（Relation Extraction）→ 图谱融合（Knowledge Fusion）。每一步依赖专用模型或规则，在封闭领域（如医疗、法律）中达到较高精度，但在开放领域面临本体论缺失和领域漂移问题。

Neural Extraction 方法（如 T5-based 联合抽取、序列标注式三元组抽取）将管道压缩为端到端模型，但训练需要大规模标注数据，且模型一旦训练完成即固定——无法处理新出现的关系类型或事实修正需求。

### 2.2 LLM-based 信息抽取

近期工作探索了用 LLM 进行零样本/小样本信息抽取。与本文的区别在于：(1) 这些工作通常聚焦于单次抽取任务（从一段文本中抽取三元组），不涉及对已有知识库的增量维护；(2) 抽取结果通常是三元组（subject, predicate, object），不含本文的节点类型标注和关系类型分类体系；(3) 没有维护判断机制——不区分"需要抽取"和"无需抽取"的输入。

### 2.3 对话记忆系统

MemGPT 将记忆管理引入 LLM Agent 的核心控制流，通过函数调用在对话上下文中插入/检索记忆。其记忆存储是扁平化的文本块，没有结构化的节点-边关系，缺乏有向联想检索所需的图拓扑支持。

Mem0 提供向量化记忆存储和检索，支持记忆的增删改查。与本文的区别在于：Mem0 没有图结构层——记忆之间的关系仅通过向量相似度隐式表达，无法支持基于关系类型的方向性联想。

Generative Agents 的记忆流（Memory Stream）包含"重要性-新近性-相关性"三维评分，但其"反思"（Reflection）机制生成的是高层抽象而非结构化图谱，且反思频率是固定间隔而非本文的 LLM 自主判断。

### 2.4 数据库维护与 DBA 隐喻

将 LLM 类比为 DBA 并非本文首创——Text-to-SQL 系统将 LLM 视为查询生成器。本文的独特之处是将 DBA 的角色从"查询执行者"扩展为"模式维护者"：LLM 不仅执行操作，还自主决定何时操作、操作什么、如何操作。这一扩展使得系统从被动工具变为主动维护者。

## 三、方法

### 3.1 记忆数据库模型

将记忆图谱建模为关系型数据库的类比：

```
关系型数据库          →     记忆图谱
─────────────────────────────────────────
表的主键              →     节点（一条独立可读的事实陈述）
外键索引              →     边（LLM 自主决定的语义关联）
DBA (数据库管理员)     →     LLM
CRUD 操作             →     create / update / fix_type / deprecate / create_edge / delete
```

LLM 不是"一次性抽取器"，而是这个数据库的 DBA——每次对话后异步执行一次维护操作。这一模型的核心优势是**增量可纠错**：如果 LLM 在某一轮标注了错误的节点类型，下一轮可以通过 `fix_type` 修正；如果连了一条不正确的边，可以通过 `delete` 删除；如果某个事实已过时，可以通过 `deprecate` 标记失效而非物理删除（保留历史追溯能力）。

### 3.2 节点与边类型体系

#### 3.2.1 节点类型（6 种）

每条记忆被标注为以下 6 种语义角色之一：

| 类型 | 判定标准 | 示例 |
|------|---------|------|
| STATUS | 用户当前所处的客观状态/处境 | "用户最近压力很大" |
| REASON | 解释为什么出现某状态/行为的原因 | "用户经常加班到晚上十点" |
| ACTION | 用户主动进行的行为/活动 | "用户靠喝咖啡提神" |
| THING | 具体的物品/地点/媒介 | "拿铁是用户最常点的咖啡" |
| PERSON | 与用户有社交关系的人 | "同事小王经常和用户一起加班" |
| EMOTION | 用户的主观情绪/感受 | "用户对加班感到疲惫" |

类型之间存在易混淆边界：STATUS vs EMOTION（客观处境 vs 主观感受）、ACTION vs REASON（用户做的事 vs 导致某事的原因）、THING vs ACTION（描述物品本身 vs 描述用户与它的互动）。System Prompt 中明确标注了这些边界规则。

#### 3.2.2 边类型（8 种）

| 类型 | 方向 | 判定 | 示例 |
|------|------|------|------|
| CAUSAL | 单向（因→果） | A 导致了 B | 加班→喝咖啡 |
| SCENARIO | 双向 | A 和 B 属于同一场景/环境 | 咖啡馆↔散步 |
| SEQUENCE | 单向（先→后） | A 发生后 B 接着发生 | 喝咖啡→散步 |
| PREFERENCE | 单向（人→对象） | 用户喜欢/偏好 B | 喝咖啡→拿铁 |
| SOCIAL | 双向 | A 和 B 涉及社交关系 | 用户↔同事 |
| ATTRIBUTE | 双向 | B 是 A 的属性/特征 | 公园↔安静 |
| TEMPORAL | 单向（事件→时间） | A 发生在 B 这个时间 | 面试→下周五 |
| TAXONOMIC | 单向（子→父） | A 是 B 的一种/子类 | 拿铁→咖啡 |

其中 CAUSAL、SEQUENCE、PREFERENCE、TEMPORAL、TAXONOMIC 五类为单向（正反向权重差异），SCENARIO、SOCIAL、ATTRIBUTE 三类为双向（正反向对称）。这一设计直接对接检索阶段的跳转轴权重矩阵——每种边类型在不同的节点类型组合上有不同的正向/反向扩展权重。

#### 3.2.3 为什么 LLM 自主决定连边

全连边的问题是 N 条记忆 = N×(N-1)/2 个潜在边，大部分没有实际联想价值。LLM 自主决定的优势：
- 只连有联想价值的边（CAUSAL 因果链、SEQUENCE 行为链）——这些是跳转轴检索真正依赖的路径
- 不连弱关联——两条记忆共享关键词但实际无关 → 不连，避免检索时噪音扩散
- 连错了可以删 ——`delete` 操作让 LLM 有纠错空间

### 3.3 LLM DBA 维护流程

#### 3.3.1 三层上下文

LLM 的维护质量取决于它能看到的上下文完整性。每次维护时，LLM 接收三层信息：

| 层 | 内容 | 为什么必要 |
|----|------|-----------|
| 对话上下文 | 本轮合并后的完整对话文本 | 理解用户意图、消解指代 |
| 记忆上下文 | 向量检索 top-15 语义相关节点 + 它们之间的已有边 | 避免重复创建、发现矛盾触发 deprecate |
| 结构上下文 | 相关节点的一跳邻居 + 图谱统计摘要 | 感知操作影响范围、避免产生孤立节点 |

三层上下文中，记忆上下文和结构上下文的规模由 `k=15` 硬限制——与图谱总节点数完全解耦，确保 prompt 大小不随记忆增长而膨胀。在 216 节点图谱上，三层上下文的 prompt tokens 稳定在约 2,000。

#### 3.3.2 操作指令集

LLM 输出 JSON 格式的操作指令：

**节点操作**：

| 操作 | 说明 | 示例 |
|------|------|------|
| `create` | 新建节点。content 为完整陈述句（已消解指代、补全省略） | `{"action": "create", "content": "用户最近因项目上线而频繁加班到晚上十点", "node_type": "REASON"}` |
| `update` | 更新已有节点的 content（事实本身变了，旧内容保留在 previous_content） | `{"action": "update", "target_id": "n12", "content": "用户最近加班到晚上十一点", "reason": "信息更新"}` |
| `fix_type` | 修正之前标错的 node_type | `{"action": "fix_type", "target_id": "n7", "node_type": "ACTION", "reason": "之前标为REASON错误"}` |
| `deprecate` | 标记节点失效，不物理删除（保留历史可追溯） | `{"action": "deprecate", "target_id": "n3", "reason": "用户已不再喝咖啡"}` |

**边操作**：

| 操作 | 说明 |
|------|------|
| `create` | 新建边。LLM 自主判断是否需要、用什么类型。只连有联想价值的 |
| `delete` | 删除之前连错的边。图检索中这条路径从此断裂 |

#### 3.3.3 维护判断

维护操作本身有成本。System Prompt 中内置了维护判断规则：

**跳过（返回空 ops）**：纯寒暄/打招呼、助手追问细节无新事实、延续话题无新信息、已覆盖。

**需要维护**：新事实、事实变化、矛盾、话题切换。

这一机制使系统在对话流中自动区分"值得维护"和"无需维护"的轮次——寒暄不累积调度器的 meaningful_count，连续 5 次空转后调度器自动跳过，零 LLM 调用。

#### 3.3.4 核心原则

System Prompt 中编码了 7 条核心原则：

1. 只抽取关于用户的事实，忽略助手的共情和寒暄
2. 第一人称转"用户"，消解指代，补全省略
3. 每条记忆独立可读，脱离对话上下文仍能理解
4. 边不需要全连，只连有联想价值的
5. 不需要一次做对所有事——错了可以下次用 `fix_type` 或 `delete` 修正
6. 如果 deprecate 一个节点，检查它的邻居是否需要重新连线
7. 发现已有节点与对话中信息矛盾时，deprecate 旧节点并创建新节点

### 3.4 GraphBuilder：操作执行与校验

GraphBuilder 是纯工程逻辑模块（不调用 LLM），负责执行 LLM 输出的操作指令：

**语义去重**：每次 `create` 前执行向量检索（k=3），若与已有节点余弦相似度 > 0.85 则跳过。这一阈值在 216 节点验证中未出现误杀。

**类型校验**：NodeType 和 RelationType 双路径解析（先匹配枚举名，再匹配枚举值），LLM 输出非标准类型名时自动纠正。

**跳转轴权重兼容性检查**：创建边前检查 `get_jump_weight(from_type, rel_type, is_reverse)`——若权重 ≤ 0（如 TAXONOMIC 正向），跳过边创建。这一检查在 LLM 输出不合理边时提供最后一道防线。

**双向边自动补全**：SCENARIO、SOCIAL、ATTRIBUTE 三类双边关系在创建正向边后，自动创建反向边——减少 LLM 的重复劳动。

**事务保护**：`_WriteTransaction` 追踪每次维护操作的所有图变更。如果 graph 写入成功但 vector_store 写入失败（如 embedding API 故障），回滚所有 graph 变更并记录错误。这一机制解决了图谱-向量库不一致的失效模式。

**孤立节点检测**：每次维护后检查所有节点。入边和出边均为零的节点被计为孤立。连续 3 次孤立标记为 `forgotten`，从活跃图中移除。

### 3.5 调度器：异步批量维护

调度器位于 DBA 上游，职责是**限流而非加速**——阻止 LLM 逐条处理对话。五层触发策略：

```
被动出口：
  - 空闲超时（60s 无对话）→ 触发
  - 会话结束 → 强制触发

主动出口：
  - DBA 累积判定 3 次"需要维护" → 进入主动模式
  - 主动模式下缓冲区每积累 6 条 → 触发

自动跳过：
  - 连续 5 次空转（DBA 返回空 ops）→ 不再调用 LLM
  - 直接清空缓冲区，零 LLM 调用
```

关键机制：
- **寒暄不累积**：DBA 判"跳过"不计入 meaningful_count，不会推入主动模式
- **单线程串行**：`_flushing` 标志 + `_pending_flush` 队列保证同时只有一次维护在执行
- **daemon 线程**：维护线程设置为 daemon=True，主进程退出时自动回收
- **停止时排空**：`stop()` 先触发最后一次维护，再 `join(timeout=120)` 等待线程结束

### 3.6 遗忘机制：边衰减而非时间衰减

传统的时间衰减（"N 天未访问 → 删除"）在图中失效——一个节点只要连着几条边，跳转轴每轮扩展都会沿边走到它，`lastAccessedAt` 被不断刷新，永远不会衰减。

本文采用**边衰减**机制——遗忘作用在边上，而非节点上，由两个机制驱动：

**机制一：LLM 边审计（主动清理）**。在每轮维护中，LLM 审计上下文中的已有边。若某条边反映的关系已被新事实覆盖（如 PREFERENCE 偏好改变、CAUSAL 因果链被否定、SCENARIO 不再处于该场景、指向已废弃节点），LLM 输出 `delete` 操作。边审计在现有 LLM 调用中顺带完成，零额外成本。实测中偏好改变场景正确删除 3 条过时边（CAUSAL + PREFERENCE + SCENARIO 双向清理），零误删。

**机制二：LLM 不作为（自然淡出）**。LLM 自主维护驱动边的生命周期：相关的记忆被反复连线，冷门话题不创建新边；旧边可能被 delete；节点边逐渐减少直至入/出度归零→孤立→连续 3 次标记为 `forgotten`。

这一机制的优势：
- **自调节**：不需要预设衰减天数。活跃话题自然保持高连接度，冷门话题自然淡出
- **与检索行为一致**：一个节点在图中的"存在感"取决于它还能不能通过联想路径到达
- **LLM 不需要主动遗忘**：遗忘是它**不做什么**的结果——不为某个节点创建新边，不修复断掉的旧边

### 3.7 图谱自组织：城市与高速公路

LLM 不需要预设场景分类。当 LLM 对每条新记忆做联想时，它会自然地把它连向同类的已有记忆——同类记忆之间连线密集，形成内部高度互联的子图（城市）；不同城市之间只有少量跨场景因果/情境连线（高速公路）。

```
[工作城市]                         [情绪城市]
  加班 ─────────────────────CAUSAL──→ 压力大
  喝咖啡 ───────────────────CAUSAL──→ 暂时放松
  颈椎疼 ───────────────────CAUSAL──→ 心情低落
                                          │
  公司楼下咖啡馆 ←──SCENARIO── 散步 ←─────┘
```

这个自组织模型与检索实验的四个核心发现完全吻合：MORE（P 找到了 A 找不到的记忆）源于城市内部因果链深度扩展；CROSS（P 跨场景输出合理联想）源于高速公路桥接；LESS（P 排除了 A 的语义误命中）源于跳转轴只在有边的城市间穿行；CROSS% 随规模下降源于城市变大后内部道路增多。

## 四、实验设置

### 4.1 测试场景

| 测试项 | 数据 | 评估方式 |
|--------|------|---------|
| 基础能力 | stress 三轮递进对话 | 人工检查节点/边正确性 |
| 增量维护 | 三轮：建图→连边→deprecate | 检查 deprecate + delete + 重建因果链 |
| 噪声对话 | 含大量寒暄的混合对话 | 事实/噪音比 5/0 |
| 错误恢复 | 5 种非标准 JSON 格式 | 降级解析成功率 |
| 大图性能 | 216 节点自然人记忆图谱 | 各阶段耗时 + LLM 操作正确性 |
| 长对话分批 | 同一主题分两批 | 跨批连边 + 去重验证 |
| 批量 vs 增量 | 三段递进历史对话 | 节点/边数 + 废弃节点 + 耗时 |
| 检索联动 | DBA 图 → P 检索 | R@all vs GT 图 |
| 调度器行为 | MockDBA 模拟 | 15 个断言（被动/主动/限流/空闲/会话结束） |
| M 最优批量 | M=3/6/9 空图 & 216 节点图 | 图质量 + 耗时 + 废弃率 |

### 4.2 环境配置

- LLM：DeepSeek-v4-flash（DBA 维护 + 维护判断）
- Embedding：BAAI/bge-large-zh-v1.5（本地 sentence-transformers 部署）
- 向量存储：FAISS（from_embeddings 增量写入）
- 图谱引擎：NetworkX DiGraph
- 核心算法：纯 Python

### 4.3 参数配置

| 参数 | 值 | 说明 |
|------|:---:|------|
| `current_state_k` | 15 | 向量检索相关节点数 |
| `dedup_threshold` | 0.85 | 语义去重余弦阈值 |
| `orphan_threshold` | 3 | 孤立节点连续计数后遗忘 |
| `proactive_entry_threshold` | 3 | 进入主动模式所需有效维护次数 |
| `proactive_batch_size` | 6 | 主动模式每批合并对话数 |
| `auto_skip_threshold` | 5 | 连续空转后自动跳过 |
| `idle_timeout` | 60s | 挂机触发等待 |
| `max_batch_rounds` | 10 | 单次维护最大合并数 |

## 五、实验结果

### 5.1 基础能力验证

三轮 stress 对话模拟验证了 LLM DBA 从零建图到矛盾处理的完整生命周期：

**第一轮后（4 节点 2 边）**：
```
n1(加班) --CAUSAL--> n2(压力大)
n3(喝咖啡) --PREFERENCE--> n4(拿铁)
```

**第二轮后（8 节点 6 边）**：新增节点自动连到已有节点——`n1(加班) --CAUSAL--> n6(去咖啡馆)` 自动连接到第一轮的节点。SCENARIO 双向边自动补全。

**第三轮后（12 节点 8 边）**：矛盾触发完整清理链——`n3(喝咖啡)` 被 deprecate，`n3→n4` 边被级联删除，`n9(胃疼) --CAUSAL--> n10(戒咖啡) --CAUSAL--> n11(改喝茶) --PREFERENCE--> n12(龙井)` 新因果链重建。

全部 10 项基础能力验证通过：

| 能力 | 验证方式 | 结果 |
|------|---------|:---:|
| 从零建图 | stress 对话 → 4 节点 2 边 | ✅ |
| 增量新增 + 自动连边 | 新节点自动连到已有节点 | ✅ |
| 双向边自动补全 | SCENARIO 双向边 | ✅ |
| 矛盾检测 → deprecate | 废弃旧节点 + 创建替代节点 | ✅ |
| deprecate 后级联 delete | n3→n4 边随 n3 废弃而删除 | ✅ |
| 节点/边去重 | 语义去重（余弦 > 0.85） | ✅ |
| 孤立节点遗忘 | 连续 3 次无连接 → forgotten | ✅ |
| fix_type 类型修正 | 单元测试覆盖代码路径 | ✅ |
| update 内容更新 | 新事实覆盖旧节点 content | ✅ |
| 噪声过滤 | 5/0 事实/噪音，正确过滤全部寒暄 | ✅ |
| 边审计（主动清理） | 偏好改变后 delete 3 条过时边，零误删 | ✅ |

### 5.2 大图性能（216 节点）

在 216 节点/266 边的自然人记忆图谱上，用一条包含冥想、晨跑、写日记的新对话触发 maintain()：

| 阶段 | 耗时 | 占比 | 优化前 | 提升 |
|------|------|:---:|:---:|:---:|
| 上下文构建 (向量检索+结构) | 0.1s | <1% | 1.79s | **18x** |
| **LLM 调用** | **~28-120s** | **~93%** | ~82s | API 波动 |
| 解析+执行 (含新节点入向量库) | 0.6s | <2% | 9.28s | **15x** |
| maintain() 核心总计 | ~30-120s | | ~98s | ~3x |

优化手段：本地 sentence-transformers 替代 SiliconFlow API（消除网络 I/O）+ FAISS `from_embeddings` 增量写入（消除重复 embedding）。

LLM 操作正确性：对话中的"冥想App→晨跑→跑鞋该换了→写日记"全部正确抽取，update 操作精确定位旧节点（跑鞋已使用快一年），边创建包括 CAUSAL/SEQUENCE/SCENARIO 多种类型，SCENARIO 双向边自动补全。

**关键发现**：
- **上下文不膨胀**：向量检索固定 k=15，prompt tokens 稳定在 ~2,000，与总节点数（216）完全解耦
- **瓶颈完全在 LLM 推理**：优化后 LLM 占 93%（优化前 84%），上下文构建和执行写入已优化到可忽略水平
- **LLM 质量不受图规模影响**：新事实正确抽取，自动连到已有节点

### 5.3 长对话分批与批量增量对比

**长对话分批**：同一主题（"压力-咖啡-想戒咖啡-改喝茶"）拆成两批。Batch 1 建基础图（6 节点 5 边），Batch 2 新增 5 节点 + deprecate 1 + 删除 2 边 + 新增 4 边。跨批连边自动连接，语义重复正确避免。

**批量导入 vs 逐轮增量**：三段递进历史对话（2024.03→2024.09）对比两种策略：

| 指标 | 批量导入 | 逐轮增量 |
|------|:---:|:---:|
| 节点数 | 8（粗粒度） | 11（细粒度） |
| 边数 | 5 | 7 |
| 废弃节点 | 0 | 1 |
| 耗时 | 79s（1次） | 161s（3次） |

逐轮增量信息保真度更高（保留独立事实而非合并），时间维度自然保留，废弃链路完整（Round3 看到变化后 deprecate 旧节点）。批量导入适合冷启动快速建立基础图谱。

### 5.4 调度器行为

用 MockDBA 模拟 skip/maintenance 返回，验证状态机转换。全部 15 个断言通过：

| 阶段 | 验证点 | 结果 |
|------|--------|:---:|
| 被动模式 | 闲聊 2 批判跳过 → meaningful_count=0 | ✓ |
| 累积 | 3 批实质性对话 → meaningful_count=1→2→3 | ✓ |
| 主动模式 | 缓冲 1/2 条不触发，3 条触发 | ✓ |
| 限流 | 15 条对话仅 8 次维护，主动模式仅 1 次 | ✓ |
| 空闲 | 0.5s 后自动触发 | ✓ |
| 会话结束 | 1 条缓冲也强制触发 | ✓ |

统计：15 条对话 → 8 次维护 → 6 次有效 + 2 次跳过。主动触发 1 次，被动触发 7 次（空闲 1 + 会话 1 + 手动 5）。

### 5.5 M 最优批量

9 条递进健身对话（入门→饮食→成果）分别以 M=3/6/9 合并为一批：

**空图场景**：

| M | 耗时 | 节点 | 边 | 秒/节点 |
|:---:|:---:|:---:|:---:|:---:|
| 3 | 110s | 9 | 0 | 12s |
| 6 | 136s | 14 | 12 | 10s |
| 9 | **87s** | 21 | **31** | 4s |

M=3 信息量不足以判断关联 → 0 条边；M=9 看到完整叙事线 → 31 条边，因果/时序链自然形成。

**216 节点已有图上（新话题「养猫」）**：

| M | 均耗时 | 均+节点 | 均废弃 | 评估 |
|:---:|:---:|:---:|:---:|------|
| 3 | — | +8 | 1.3 | 信息不足，易过度反应 |
| 6 | **111s** | +8.3 | **1.3** | 最优：耗时稳定，2/3 轮零误 deprecate |
| 9 | 205s | +10.7 | 3.0 | 图更全但耗时翻倍、废弃率高 |

**结论：M=6 为生产默认值**——空图建图质量好，已有图上性能稳定、误 deprecate 率低。

### 5.6 检索联动验证

端到端测试验证了 DBA 自动构建的图谱与人工标注 GT 图谱的检索等价性：

- 节点召回率 69.2%，精确率 90%
- DBA 图上的 P 检索 R@all = 100%，与 GT 图持平

DBA 图与 GT 图在节点粒度上有差异（DBA 可能将一个事实拆成两个节点，GT 可能合并），但检索效果等价——跳转轴的因果/时序扩展路径在两种粒度下都能正常运作。这证明：**前置数据管理的质量标准不是"精确复现人工标注"，而是"产出的图谱使下游检索达到同等效果"。**

## 六、讨论

### 6.1 随 LLM 能力提升的自然增益

DBA 架构的核心设计原则之一是将最难的部分外包给 LLM，因此 LLM 自身的进步会直接、零成本地提升系统表现。

**质量维度**（模型理解力 ↑）：事实抽取精度、节点类型标注、边类型判定、矛盾检测、指代消解、维护判断 — 全部受益。

**延迟维度**（推理速度 ↑）：LLM 调用占链路 93%，延迟等比例下降；M 最优值可上调 → 更大批量 → 更少调用次数。

**成本维度**（推理价格 ↓）：价格下降直接降低单次维护成本，成本降低后可增大 M → 更高图质量。

**不随 LLM 进步的部分**（架构固定开销）：FAISS 向量检索（算法复杂度固定）、图存储与遍历（纯数据结构）、调度器状态机（规则逻辑）、语义去重与校验（本地计算，已优化到 0.6s）。这些部分已优化到可忽略水平，不构成系统瓶颈。

关键设计启示：DBA 架构中不做额外的规则引擎、分类器、消歧模块——这些在 LLM 能力提升后都会成为技术债。当前架构中围绕 LLM 的部分（上下文构建、操作解析、事务保护）都是为了**让 LLM 发挥最大价值**，而非替代 LLM。

### 6.2 已知局限

1. **LLM API 波动**：DeepSeek 响应时间不稳定（28s~120s），是整条链路的主导瓶颈（占 93%）。调度器批量累积（M=6）和自动跳过机制缓解了部分压力，但 LLM 延迟仍是硬约束。

2. **数据集人工构造**：所有验证在 YAML 人工标注数据上完成，真实对话日志中的抽取质量待验证。人工构造数据适合机制验证，但无法反映真实使用中的自然语言噪音和对话结构复杂性。

3. **fix_type 极少被触发**：LLM 抽取质量高，实际很少需要修正类型。代码路径已验证但不构成主要使用场景。这本身是好事（LLM 类型标注质量高），但意味着纠错能力的真实价值在更大规模/更多噪声的数据上才能体现。

4. **单用户设计**：当前架构假设单用户图谱。多用户场景需要用户维度的图谱分区和调度器实例隔离。

### 6.3 与检索阶段的管道价值

DBA 输出的图谱质量直接决定检索阶段的上限。检索实验已证明：跳转轴是召回深度的关键条件（无跳转轴 → R@all 回归纯向量水平），TAXONOMIC 阻断将跨域噪音从 44% 降至零，节点类型和边方向的标注精度直接影响检索阶段的方向性信号。

DBA 架构中提供的 `fix_type` 和 `delete` 操作不仅是维护功能，更是检索质量的持续保障机制——一条错误的边类型标注可能导致跳转轴沿错误方向扩展，`delete` 提供了纠错空间；一个错误的节点类型会导致权重矩阵匹配失败，`fix_type` 提供了修正能力。

全链路数据流如下：

```
┌──────────────┐    ┌──────────┐    ┌───────────┐    ┌──────────┐    ┌──────────┐
│  对话日志     │──→│  DBA     │──→│  MemoryGraph │──→│  P 检索  │──→│ StoryRank │──→│  回复
│  (非结构化)   │   │  维护    │   │  + VectorStore│   │  (联想)  │   │ (故事化)  │   │  生成
└──────────────┘    └──────────┘    └──────────────┘    └──────────┘    └──────────┘
    异步后台          1次LLM/批       TAXONOMIC阻断      peak容忍带      故事片段   1次LLM
```

### 6.4 StoryRank：检索链路的故事化

P 检索通过峰值容忍带返回一组候选记忆，这些候选并非彼此孤立的语义近邻，而是由有向边连接、带有关系类型标注的因果链路。在送入 LLM 生成回复前，需要把这条链路理解并整理成可供聊天模型直接接收的「记忆片段文档」，原因有三：(1) 原始 `[id] content` 节点列举会污染聊天 LLM 的上下文、稀释其注意力；(2) 检索阶段的 `combined_score` 已证明排序精度不如纯向量（P@5=0.160 vs 0.267）；(3) 扁平列表会丢失检索链路的因果关系，而因果关系正是人类联想的核心。

本文采用 **StoryRank**——一次 LLM 调用，把「节点 + 关系」构成的检索因果链路理解并整理成连贯的故事片段，而非对扁平列表打分重排。它与传统 rerank 的本质区别在于：rerank 对候选做**逐项打分排序**，输出仍是扁平的 `[id] content` 列表；StoryRank 则对候选做**结构化理解与重写**，输出一段保留因果结构的自然语言故事，并在理解过程中完成语义过滤。

#### 6.4.1 三个职责

1. **保留因果关系**：关系类型（CAUSAL / PREFERENCE / SCENARIO / SEQUENCE 等）语义自然融入故事句子（「因为…」「于是…」「偏爱…」），不丢失检索链路的因果与关联结构。
2. **前置整理记忆片段，避免污染聊天 LLM 上下文**：聊天模型只接收干净的故事段落，而非 `[id] content` 节点列举。
3. **对检索质量做一次语义过滤**：LLM 按边关系组织故事时，明显突兀、与主线无关的节点被自然舍弃，不纳入故事。

#### 6.4.2 算法流程

StoryRank 的完整链路为「检索 → 连通性粗筛 → 路径构建 → LLM 故事化 → 汇总」：

1. **输入**：峰值容忍带返回的候选集合及其扩展轨迹。检索阶段通过 `expand_with_trace` 为每个候选记录父节点 `from`、关系类型 `rel_type` 与方向 `is_reverse`，使候选之间构成一棵以种子为根的**扩展树**（回访时保留首次出现的父边，避免父指针成环）。
2. **连通性粗筛（`select_core_nodes`）**：从扩展树中提取父子关系，将每个结果节点沿 `from` 回溯到根（种子节点），按「共享根」把候选拆分为若干条核心链路；组内节点再按 hop 顺序（根 → 叶）排列。
3. **路径构建（`_build_path`）**：将每条核心链路整理为 `{nodes, edges}` 结构，节点携带 `id/content/node_type`，边携带 `from/to/rel_type/is_reverse`，作为 LLM 的结构化输入。
4. **LLM 故事化（`story_rank`）**：将用户查询与路径文本送入 LLM，由 LLM 输出一段连贯故事与采纳的节点 `adopted_ids`。
5. **汇总**：系统据此汇总 `stories`（故事文本）、`story_nodes`（采纳节点）与 `discarded_nodes`（舍弃节点）。

#### 6.4.3 关系语义映射

故事化的关键在于**把类型化边翻译成自然语言连接词**，使因果与关联结构在句子层面被保留而非被扁平化。Prompt 中显式给出了 8 种边类型的语义映射：

| 边类型 | 故事化连接词 |
| ------ | ------------ |
| CAUSAL（因果） | 因为 / 导致 / 所以 |
| SEQUENCE（时序） | 然后 / 之后 / 接着 |
| PREFERENCE（偏好） | 偏爱 / 喜欢 |
| SCENARIO（场景） | 在…场景下 / 常去 |
| ATTRIBUTE（属性） | 具有…特征 |
| TEMPORAL（时间） | 时间上 |
| SOCIAL（社交） | 与…有关 |
| TAXONOMIC（分类） | 属于 / 是一种 |

#### 6.4.4 Prompt 设计与语义过滤

StoryRank 的 Prompt 以「严格 JSON」约束输出，并同时承担三项约束：(1) 只输出一段连贯文字，不机械列举；(2) 只采纳与链路连贯的记忆，明显突兀、与主线无关的节点应舍弃；(3) 忠实于节点内容，不编造未出现的事实。LLM 输出的 `adopted_ids` 经系统校验后，将候选划分为「采纳」与「舍弃」两类——被舍弃的节点正是那些在故事组织中无法自然融入主线的记忆。若 LLM 输出无法解析为 JSON，则降级为拼接节点内容、全部采纳，保证链路在异常下仍可用。

#### 6.4.5 与检索三机制的衔接

StoryRank 是 P 检索链路的下游收尾环节：跳转轴决定「往哪个方向走」，目的回归保证「不偏离动机」，峰值容忍带决定「何时停下并取回哪些候选」，StoryRank 则把取回的候选链路「翻译成可被聊天模型直接消费的故事」。前三者解决的是**召回与方向**问题，StoryRank 解决的是**表达与去噪**问题，四者共同构成「检索 → 故事化 → 回复」的完整闭环。

#### 6.4.6 与 DBA 图结构的关系

StoryRank 的故事化质量直接依赖于 DBA 图谱中节点类型和边类型的标注质量。一条正确的 CAUSAL 边使「压力大 → 加班 → 喝咖啡」在故事中被自然串成因果链；一条缺失的 SCENARIO 边使「咖啡馆」与主线脱节而被舍弃。DBA 的 `fix_type` 和 `delete` 同样保障着故事化的准确性。

#### 6.4.7 验证

在 5 节点因果链（压力大 → 加班 → 喝咖啡 → 拿铁 → 猫咪咖啡馆）上，StoryRank 输出连贯故事——「用户压力很大，因为经常加班到10点，于是喝咖啡提神，偏爱拿铁，常去公司楼下的猫咪咖啡馆」，关系语义自然融入，5 节点全部采纳。

## 七、本部分小结

本部分提出了 LLM DBA 架构——将记忆图谱构建从"一次性 NLP 抽取"重新定义为"LLM 持续自主维护的数据库"。核心贡献包括：

1. **记忆数据库模型**：节点是主键记录、边是外键索引、LLM 是 DBA——增量可纠错的 CRUD 操作集（create/update/fix_type/deprecate + create/delete）。

2. **三层上下文 + 维护判断**：对话、记忆、结构三层信息使 LLM 能自主判断是否需要维护，避免对寒暄的无效 LLM 调用。

3. **完整的工程体系**：调度器（缓冲累积 + 五层触发 + 自动跳过）、GraphBuilder（语义去重 + 类型校验 + 事务保护 + 孤立遗忘）、三层上下文构建（向量检索 k=15 + 图结构邻域）。

4. **9 项集成验证**：覆盖从建图到检索联动的完整链路。增量维护验证了 deprecate + delete + 重建因果链的完整生命周期；检索联动中 DBA 图 R@all 与 GT 图持平（100%）；调度器 15/15 断言通过；M=6 确定为生产默认批量。

5. **性能优化**：P1/P2 优化将非 LLM 部分压缩至 0.7s（18x+15x），LLM 推理成为唯一瓶颈（93%）。优化手段包括本地 embedding 部署、FAISS 增量索引、事务保护。

本部分的核心设计哲学是"让 LLM 做最难的部分"——不做额外的规则引擎、分类器或消歧模块。这些组件在 LLM 能力提升后都会成为技术债，而 LLM-native 架构随模型进步零成本增益。下游的 **StoryRank**（§6.4）进一步将记忆选择从"语义匹配"校正为"故事化的联想理解"，其关系类型（因果链、偏好、情境等）直接依赖 DBA 图谱的边结构质量，构成从前置数据管理到最终回复的完整闭环。

---

# 第二部分：检索理论——目的驱动的联想记忆检索模型

## 一、引言

### 1.1 问题定义

对话系统的长期记忆检索目前主要采用两种范式：**语义向量检索**将用户查询与记忆文本做嵌入相似度匹配；**知识图谱检索**从查询中提取实体，沿图关系扩展邻接记忆。许多生产系统采用二者混合的策略（如 RRF 融合）。

两种范式共享一个隐含假设：**记忆关联强度等于语义相似度或图结构距离**。这一假设的失效场景见"系统总览"中的"压力大 → 意大利咖啡文化"示例——关键缺失在于**方向感**。在人类联想中，"压力大"沿**因果轴**走到"加班"，再沿**行为轴**走到"喝咖啡"，再沿**情境轴**走到"安静咖啡馆"——每一步都有明确的方向，整个联想链指向一个隐含目的（"找到能缓解压力的方式"）。当前系统使用无向边或多跳扩展，不区分扩展方向，不检验每步与原始目的的关联，不判断何时该停止。

### 1.2 核心构想与本部分贡献

本部分的核心假说是：记忆检索可以被重新定义为**有向的联想搜索**，而非简单的语义匹配。基于这一假说，我们提出三个协同机制：

1. **跳转轴模型**：将知识图谱边类型化为 8 种关系类型，定义 6×8 的节点-边类型权重矩阵，以扩散激活理论为认知基础提供有向扩展的方向性控制。
2. **目的回归机制**：从用户查询中推断隐含目的并编码为目的向量，在联想链扩展过程中约束方向、防止偏离原始动机。
3. **峰值容忍带终止策略**：基于目的关联度的斜率变化判断扩展终止点，从峰值附近一个容忍常数范围内的多轮候选中截取输出，替代固定 top-K 和单轮峰值截断。

本部分的实验贡献包括：

4. **组件维度分化的消融证据**：在 47/83/216 节点上完成 8 组消融的 N 线统一重跑，A2（无跳转轴）在大图上退化为纯向量水平，确认跳转轴是召回深度的关键条件；目的回归的子机制分工明确；TAXONOMIC 阻断将开放后的干扰率从 44% 降至零。
5. **行为维度与 CROSS 双指标评估**：揭示了 MORE/LESS/CROSS 三个正交维度及其双因子驱动模型；通过 CROSS_VALID 双指标系统纠正了此前基于 expected 集的"噪音率"评估偏差，P 的跨域合理联想是 A 的 6.4 倍。
6. **召回优势的规模单调性验证**：P/A 比值序列 2.1x(47)→2.8x(100)→3.7x(150)→3.4x(216)，确认优势随规模放大。

本部分剩余内容组织如下：§2 回顾相关工作。§3 描述三个核心机制及完整算法。§4 实验设置。§5 实验结果，按论证主线展开——先验证召回深度（§5.1 聚合指标、§5.2 消融实验、§5.3 规模效应），再分析 CROSS 跨场景联想（§5.4），最后讨论行为维度（§5.5）。§6 讨论设计性质、局限与管道价值。§7 小结。

## 二、相关工作

现有对话记忆检索方法可大致分为三类：基于向量的语义匹配（如 MemGPT）、基于图谱的结构扩展、以及二者的混合。基于向量的方法受限于语义空间的分布——当"缓解压力"和"喝咖啡"在嵌入空间中的余弦相似度仅约 0.2-0.3 时（以 BAAI/bge-large-zh-v1.5 为例），单独的语义检索难以覆盖功能性相关的记忆。基于图谱的方法虽然能沿关系遍历，但无向的 BFS/DFS 扩展缺乏对扩展方向的判断——从 STATUS 节点出发时，沿 REVERSE_CAUSAL（"是什么导致的"）扩展与沿 TAXONOMIC（"是什么的上级类别"）扩展在传统图谱检索中被同等对待。

Generative Agents 在记忆检索中引入了"重要性-新近性-相关性"的三维评分，但其相关性评分仍基于纯语义嵌入，未区分不同类型关系的方向性差异。

**GraphRAG 系列方法**（GraphRAG, LightRAG）将知识图谱与 RAG 结合，通过社区检测或图遍历从非结构化文本中提取结构化知识增强 LLM 回答。与本文的区别在于：(1) GraphRAG 侧重全局社区摘要，本文关注个体记忆节点间的有向联想路径；(2) LightRAG 的图遍历使用无向双端检索，本文的跳转轴为有向且带类型化权重。

**扩散激活理论**（Spreading Activation）是认知心理学中联想记忆的经典模型——从一个概念节点出发，激活沿加权连接扩散到相关概念。本文的跳转轴直接继承这一框架，并将其具体化为 6×8 的节点-边类型权重矩阵。与自由联想中的单一激活扩散不同，本文引入目的回归作为激活的约束信号，使扩散方向受查询隐含目的引导。

**检索多样性控制**（MMR）在传统信息检索中用于平衡相关性与新颖性。本文的峰值容忍带终止在概念上类似于 MMR 的多样性约束——从峰值附近的多个轮次采集候选而非仅取峰值单轮。但动机不同：MMR 追求主动去冗余，容忍带追求"不错过深层才出现但高价值的跨域路径"。

**知识图谱路径排序**在 KG 推理中通常依赖预训练的路径编码器或图神经网络。本文采用更轻量的方案：跳转轴权重 × 距离衰减 × 目的关联度，避免了对大规模路径标注数据的依赖，在人工构造的小规模数据集上即可实现有效排序。

**LLM-native 记忆检索**（如 MemGPT、Mem0）将记忆文本与用户查询一并送入 LLM，由 LLM 直接做 relevance ranking。这类方法在数百条记忆的规模上 API 调用可行，但本文未将其作为基线，原因有三：(1) LLM 语义排序无法区分语义邻近与因果邻近——"喝了拿铁因为朋友推荐"和"喝了拿铁因为加班需要提神"在文本空间中高度相似，但只有后者与"缓解压力"功能相关；(2) 当候选记忆池中大量文本共享关键词，LLM 的 relevance score 可能坍缩到窄带，正确记忆与噪音记忆无法有效拉开——这恰是跳转轴通过关系类型过滤要解决的核心问题；(3) LLM 排序存在方差大、位置偏差、长度偏差等问题。本文的定位是：Proposed 方法与 LLM-native 排序并非替代关系，而是**上下游协作关系**——Proposed 作为检索阶段从大规模记忆库中召回候选集（侧重召回深度），LLM 可在其后对候选集做重排序或直接基于候选记忆生成回复（侧重排序精度与生成质量）。本文聚焦检索阶段的召回改进。

## 三、方法

### 3.1 概述

将记忆检索从"匹配"重新定义为**有向的联想搜索**：

```
输入：用户查询 q
步骤 1：推断 STATUS 和隐含目的 purposes
步骤 2：向量搜索种子记忆 M₀
步骤 3：循环扩展（直到寻峰终止）：
  - 从当前记忆集 Mₙ 沿有向边按跳转轴规则扩展
  - 每步计算目的关联度 purpose_score
  - 过滤低于阈值的候选
  - 斜率转负时停止，回到峰值容忍带截段
步骤 4：输出候选记忆集
```

### 3.2 跳转轴：有向类型化扩展

#### 3.2.1 边的类型化

传统知识图谱的关系边是文本标签（如"导致"、"偏好"），系统难以区分语义类别。我们将边定义为 8 种基本关系类型：

| 类型 | 说明 | 方向性 |
|------|------|--------|
| CAUSAL | 因果关系 | 指向结果 |
| SCENARIO | 场景/环境归属 | 双向 |
| SEQUENCE | 时序先后 | 指向后序 |
| PREFERENCE | 态度/偏好 | 指向对象 |
| SOCIAL | 社交关系 | 双向 |
| ATTRIBUTE | 属性归属 | 双向 |
| TEMPORAL | 时间定位 | 指向时间 |
| TAXONOMIC | 分类/本体 | 指向父类 |

#### 3.2.2 权重矩阵

节点按其语义角色分为 6 种类型：STATUS、REASON、ACTION、THING、PERSON、EMOTION。每个节点类型在扩展时，对不同关系边类型有不同的正/反向权重：

| NodeType → | CAUSAL | SCENARIO | SEQUENCE | PREFERENCE | SOCIAL | ATTRIBUTE | TEMPORAL | TAXONOMIC |
|-----------|--------|----------|----------|------------|--------|-----------|----------|-----------|
| STATUS    | 0.9/1.0| 0.8/0.8  | 0.7/0.0  | 0.4/0.0    | 0.5/0.5| 0.3/0.3   | 0.3/0.0  | 0.0/0.0   |
| REASON    | 1.0/0.9| 0.6/0.6  | 0.8/0.0  | 0.2/0.0    | 0.4/0.4| 0.3/0.3   | 0.3/0.0  | 0.0/0.0   |
| ACTION    | 0.9/0.7| 0.8/0.8  | 0.9/0.6  | 0.5/0.0    | 0.6/0.6| 0.4/0.4   | 0.5/0.0  | 0.0/0.3   |
| THING     | 0.3/0.3| 1.0/1.0  | 0.4/0.4  | 0.8/0.0    | 0.3/0.3| 0.7/0.7   | 0.3/0.0  | 0.0/0.3   |
| PERSON    | 0.5/0.5| 0.6/0.6  | 0.5/0.5  | 0.8/0.0    | 1.0/1.0| 0.7/0.7   | 0.4/0.0  | 0.0/0.0   |
| EMOTION   | 0.9/1.0| 0.8/0.8  | 0.6/0.0  | 0.5/0.0    | 0.5/0.5| 0.4/0.4   | 0.3/0.0  | 0.0/0.0   |

> 格式：`正向/反向`，0 表示阻断该方向的扩展。8 种关系类型中，CAUSAL、SEQUENCE、PREFERENCE、TEMPORAL（反向统一阻断）、TAXONOMIC 五类正向/反向权重存在差异，承担方向性判别；SCENARIO、SOCIAL、ATTRIBUTE 三类正反向对称，权重因节点类型而异。

核心设计原则：**每个跳转的方向必须与出发点有关。** STATUS 节点的正确扩展方向是原因（REVERSE_CAUSAL=1.0）、情境（SCENARIO=0.8）和后续行为（CAUSAL=0.9），而非分类学上位概念（TAXONOMIC 正向=0.0）。TAXONOMIC 正向（指向父类）权重为零是结构性阻断——情绪/状态驱动的问题不应沿分类学关系漂移到无关的上位概念。反向（指向子类）对 ACTION/THING 节点开放 0.3，允许从父类检索具体的子类实例。

#### 3.2.3 权重设计的认知基础

权重矩阵基于扩散激活理论手工设计：激活沿语义网络中的连结扩散，强度随距离衰减。本文在此基础上进一步引入关系类型特异化权重——不同类型的边承载不同的激活强度。这一原则被映射为三条序数约束：

- **因果关系的激活优先于情境关系**（CAUSAL > SCENARIO）——人们更自然地沿着"什么导致了什么"的方向联想
- **情境关系优先于时序关系**（SCENARIO > SEQUENCE）——环境信息比时间顺序更具联想粘性
- **分类学关系在情绪/状态驱动联想中被抑制**（TAXONOMIC = 0）——"压力大"不应联想到"压力的神经科学分类"

类型化权重通过**差异化的激活强度**使不同关系类型的路径获得不同权重，其核心效果体现在两个层面：(1) **方向性阻断**（如 TAXONOMIC 正向=0）结构性剪枝无关路径；(2) **路径差异化排序**使因果链上的节点获得更高综合得分。消融实验（§5.2）表明，等权扩展在小图上输出反而更收敛（16.3 vs 18.9），说明类型化权重的作用不是减少输出总量——而是在不牺牲召回的前提下为后续排序提供更细粒度的得分区分，同时通过方向性阻断控制噪音。具体数值的序数关系（如 0.9 vs 0.8 vs 0.7）被距离衰减和目的加权的双重作用压缩，同序相邻数值在实际综合得分中的差异 < 0.02，精确调校的边际价值极小。消融实验通过 A1（等权化）和 A2（归零）逐项验证了类型化与方向性的独立贡献。

### 3.3 目的回归：保持不偏离

跳转轴提供方向性引导，目的回归保证不偏离出发动机。

#### 3.3.1 目的推断

从用户当前消息推断隐含目的（使用轻量 LLM 调用）：

```
用户："最近压力太大了"
→ purposes = ["倾诉", "缓解", "寻求安慰"]
→ 目的向量 = embedding("倾诉 缓解 寻求安慰")
```

目的词数量（宽度）是可调参数：过窄导致不同缓解行为的余弦被压缩到同一窄带，丧失排序区分度；过宽导致目的向量泛化，失去过滤能力。通过 dose-response 实验（§5.4.3）在 63 节点异构数据集上确定最优宽度为 2 词。**此最优值依赖于数据集的场景多样性**——在低多样性数据集上宽度效应消失（§5.5.2），不可跨数据集泛化。本文以 2 词为实验默认值。

#### 3.3.2 每步校验

```
第 n 轮扩展：
  对每个候选记忆 c：
    purpose_score = cosine(embedding(c), 目的向量)

  过滤：purpose_score < 0.2 → 丢弃

  最终得分 = 跳转轴权重 × 0.85^hop × 0.5 + purpose_score × 0.5
```

目的过滤阈值设为 0.2 而非初始设计的 0.3，是因为 bge-large-zh-v1.5 对"缓解"与"喝咖啡"的余弦相似度约在 0.2-0.3 之间。阈值 0.3 会误杀大部分跳转轴扩展的功能等价结果。

### 3.4 峰值容忍带终止：替代固定 top-K

传统检索以固定 top-K 截断，但联想过程的相关性不是单调的——它会先上升到达峰值，然后下降。

```
目的关联度 μ
    ↑
    |        ● ← 峰值
    |      ／＼
    |    ／    ＼
    |  ／        ＼
    └────────────────→ 扩展轮次
       上升段    下降段
```

初始设计中，寻峰在判定下降后回到峰值单轮，取其候选作为最终输出。但在实验中我们发现这一策略存在根本缺陷：**峰值轮几乎总是锁定在第 1 跳**——因为种子轮使用纯 purpose_score（0.4-0.6 量级），而扩展轮引入 jump_weight 加分后均值必然跳涨，形成「伪峰值」。这意味着所有距离种子 2 跳以上的节点永远被丢弃，无论其关联质量如何。

我们将策略修正为**峰值容忍带截段**：不再取峰值单轮，而是收集均值在 `[peak_μ - δ, peak_μ]` 范围内的所有轮次的候选节点（去重）。容忍常数 δ = 0.10。

实际轨迹示例（来自 216 节点"工作压力大怎么放松"查询）：

```
Hop 0  seed          μ=0.557  ← purpose_score only
Hop 1  跳转轴扩展     μ=0.861  ↑ 峰值 (jump_weight 首次引入)
Hop 2  跳转轴扩展     μ=0.820  ↓ δ=0.041 < 0.10 → 纳入容忍带
Hop 3  跳转轴扩展     μ=0.798  ↓ δ=0.063 < 0.10 → 纳入容忍带
Hop 4  跳转轴扩展     μ=0.738  ↓ δ=0.123 > 0.10 → 截断
```

δ 的敏感性：对 δ ∈ {0.05, 0.10, 0.15, 0.20} 在 47 和 216 节点上做参数扫描。δ=0.05 在 216 节点上 R@all 降至 0.500（截断过严），δ=0.15-0.20 增加输出量 15-58% 但 R@all 边际收益仅 +0.003-0.007。δ=0.10 为 R@all-输出量 Pareto 最优点，作为默认值固定。CROSS 案例验证了容忍带的实际效果（§5.4.4）：2-3 跳才出现的候选在单轮峰值截断时被丢弃，容忍带截段后首次命中，R@all 从 0.556 提升至 0.667。

### 3.5 完整算法

```
Algorithm: Purpose-Driven Associative Retrieval (v2)

Input:  用户查询 q
Output: 容忍带内的候选记忆集 R

1.  status, purposes ← InferStatus(q)
2.  purpose_vec ← Embed(purposes)
3.  M₀ ← VectorSearch(q, k)
4.  μ₀ ← mean(purpose_score(M₀, purpose_vec))
5.  seen_peak ← False, extra_rounds ← 0

6.  for hop = 1 to max_hops:
7.    C ← Expand(M_{hop-1}, jump_axis_rules)
8.    C ← filter(C, purpose_score > 0.2)
9.    C ← C × 0.85^hop × 0.5 + purpose_score × 0.5
10.   μ_hop ← mean(combined_score(C))

11.   if |μ_hop - μ_{hop-1}| < 0.015:       // 持平
12.     M_hop ← C; continue
13.   if μ_hop > μ_{hop-1}:                 // 上升
14.     seen_peak ← True; peak_hop ← hop; peak_μ ← μ_hop
15.     extra_rounds ← 0                    // 新峰值重置下降计数
16.     M_hop ← C
17.   else:                                  // 下降
18.     extra_rounds++
19.     if seen_peak and extra_rounds > 2:
20.       // 峰值容忍带截段（替代点峰值）
21.       for each hop h:
22.         if μ_h ≥ peak_μ - 0.10:
23.           R ← R ∪ candidates_h (去重)
24.       return R
25.     M_hop ← C

26. // 未触发寻峰（如首跳即低于种子）：退回全局峰值 + 容忍带截段
27. if not seen_peak:
28.   peak_μ ← max(μ_0, ..., μ_{max_hops})
29.   for each hop h:
30.     if μ_h ≥ peak_μ - 0.10:
31.       R ← R ∪ candidates_h (去重)
32. return R
```

## 四、实验设置

### 4.1 基线方法

| 方法 | 向量检索 | 图扩展 | 边方向 | 目的回归 | 终止 |
|------|---------|--------|--------|---------|------|
| **A** (纯向量) | ✅ | — | — | — | top-K |
| **B** (向量+图谱混合) | ✅ | 无向多跳 | 无向 | — | 固定3轮 |
| **C** (跳转轴 only) | ✅ | 有向 | 跳转轴 | — | 固定3轮 |
| **P** (完整链路) | ✅ | 有向 | 跳转轴 | ✅ | 寻峰 |

### 4.2 数据集

实验按六阶段推进，逐步扩大数据规模与场景多样性：

| 阶段 | 节点/边 | 场景数 | 场景构成 | 查询数 |
|------|--------|--------|----------|--------|
| 一（概念验证） | 14 / 17 | 1 | stress | 3 |
| 二（三场景合并） | 47 / 49 | 3 | stress + mood + leisure | 9 |
| 三（五场景扩展） | 83 / 87 | 5 | + designer + writer | 13 |
| 四（跨场景控制） | 63 / 70 | 4 | stress + mood + health + learn | 10 |
| 五（自然人记忆） | 216 / 266 | 6 | work + health + social + learning + mood + noise | 15 |
| 六（收敛与边界） | 47—216 | 4—27 | 复用前五阶段数据集 | 15—54 |

所有数据为人工构造的 YAML 格式，每条记忆标注节点类型（STATUS/REASON/ACTION/THING/PERSON/EMOTION），每条边标注关系类型（8种之一）。人工构造的原因是控制实验需要精确标注类型与方向，排除对话噪音与标注歧义对因果推断的干扰。**数据的人工构造性质是本文的核心局限之一**——所有结论均在"精确标注"的理想条件下成立，真实对话日志中自动抽取的图结构和节点类型可能引入额外的误差来源，详见 §6.1 第 3 项。

### 4.3 评估指标

- **P@5 / R@5**：与 A/B 可比的精度基准
- **P@all / R@all**：P 全部输出中预期记忆的命中率——由于 P 方法通过容忍带截段输出 5-37 条记忆（A/B 固定 5 条），R@all 为主指标，P@5 为参考
- **MRR**：第一个相关记忆排名的倒数均值
- **独家命中数**：仅 P 命中而 A/B/C 均未命中的预期记忆数
- **CROSS_OUT**：P 输出中 domain ≠ query_domain 的节点数/率
- **CROSS_VALID**：CROSS_OUT 中经 LLM 标注为 ✓（功能等价）或 ✓+~（功能等价 + 概念相关）的节点数/率——替代了此前基于 expected 集的"干扰率"指标
- **CROSS_R**：P 输出中不在 expected 内但功能等价（✓）的跨域节点数，独立于 R@all

### 4.4 参数配置

| 参数 | 值 | 说明 |
|------|-----|------|
| `purpose_filter_threshold` | 0.2 | 目的余弦过滤阈值 |
| `jump_weight` : `purpose_weight` | 0.5 : 0.5 | 组合得分权重 |
| `patience` | 2 | 寻峰容忍连续下降轮数 |
| `min_delta` | 0.015 | 斜率敏感度 |
| `max_hops` | 5 | 最大扩展跳数 |
| `seed_k` | 5 | 种子记忆数 |
| `peak_tolerance` | 0.10 | 峰值容忍常数 |
| `distance_decay` | 0.85 | 每跳衰减因子 |

### 4.5 环境配置

- LLM：DeepSeek-v4-flash（目的推断与状态推断）
- Embedding：BAAI/bge-large-zh-v1.5
- 向量存储：FAISS
- 图谱引擎：NetworkX DiGraph
- 核心算法：纯 Python，不依赖框架

## 五、实验结果

### 5.1 召回深度：聚合指标

| 阶段 | 规模 | A P@5 | B P@5 | P P@5 | P R@all | A R@all | P vs A(R@all) |
|------|------|--------|--------|--------|---------|---------|:---:|
| 一（概念验证） | 14节点 | — | — | — | — | — | 定性验证：C>A/B |
| 二（三场景合并） | 47节点 | 0.422 | 0.422 | 0.311 | 0.835 | 0.401 | **+108%** |
| 三（五场景扩展） | 83节点 | 0.323 | 0.323 | 0.200 | 0.868 | 0.359 | **+142%** |
| 五（自然人记忆） | 216节点 | 0.267 | 0.267 | 0.160 | 0.538 | 0.159 | **+238%** |

> B 的 P@5 在所有实验规模上与 A 完全一致（A = B），单独列出以强调无向图谱扩展不提供任何增量价值。C（跳转轴 only）在 47 节点 R@all=0.323、83 节点 R@all=0.279、216 节点 R@all=0.267，各规模均介于 A 和 P 之间，为有向扩展提供中间参照基线。
>
> **A=B 的技术原因**：B 的 RRF 融合使用 α=0.5。BFS 扩展的节点图谱分约为 0.25-0.33，而向量种子得分约 0.9-1.0，无向图扩展节点无法进入 Top-5。本文保留此参数化以说明：**仅有无向图结构、没有方向性权重，在可比条件下难以产生不同于纯向量的结果。**

**五个关键发现**：

1. **有向边能找无向边找不到的记忆（阶段一）**：在 14 节点"压力大"场景中，A/B 仅命中语义层面的记忆，C 通过有向边沿因果链走到"喝咖啡→散步→咖啡馆"——这些缓解行为与查询无语义关联。

2. **在我们的实验配置下 A = B 跨规模完全一致**：在典型混合检索配置下，仅有无向图结构而不区分边方向与权重，难以产生不同于纯语义向量的检索结果。

3. **P 的 R@all 在全规模上远超 A，优势根源于图结构信号而非输出量差异**：P 的输出量（6-27 条）显著大于 A（固定 5 条），但 A2 消融（无跳转轴）提供了干净的因果证据——A2 与 P 在输出机制上完全相同（同样的目的过滤、寻峰终止），仅差跳转轴，A2 在 216 节点上 R@all 与 A 持平（0.159），独家命中为 0（§5.2）。**代价是 Top-5 排序精度**：P 的 P@5 在 216 节点上为 0.160，低于 A 的 0.267——Proposed 适用于"多轮对话中提供丰富的记忆上下文"场景，而非精确排序场景。

4. **P 的绝对 R@all 随规模下降，但相对 A 的优势在更大规模上扩大**：P 对 A 的相对优势从 47 节点的 2.1 倍扩大至 216 节点的 3.4 倍——不是因为 P 变强了，而是因为纯语义检索在大图上退化得更快。

5. **TAXONOMIC 阻断在基本实验中零命中 Top-5**：在 47/63/83 节点的全部 32 条查询中，分类学扩散链零命中 Top-5，证明跳转轴的结构性阻断在跨规模条件下稳定运作。

**关于 R@all 比较的说明**：P 的输出量（6-27 条）显著大于 A（固定 5 条），R@all 在输出配额上不对等。本文选择不通过扩大 A 的 k 来做"输出量对齐"——A 作为纯语义向量检索方法，其核心假设是向量相似度排序靠前的记忆最相关；扩大 k 等同于纳入 A 自身置信度递减的低分候补。论证 P 的优势不依赖输出量对齐，而依赖 **A2 消融**（无跳转轴）：A2 与 P 共享相同的输出机制和输出配额，仅差跳转轴。A2 在 216 节点上 R@all 与 A 持平、独家命中为 0——这直接证明了 P 的召回优势来自跳转轴的图结构信号，而非输出量膨胀。

### 5.2 组件贡献：消融实验

在 47 和 216 节点上对 P 的三个组件分别做 8 组消融，量化独立贡献。所有数据来自 N 线统一评估框架（R@all 为主指标）：

| ID | 消融项 | R@all(47) | vs A(47) | R@all(216) | vs A(216) | 作用维度 |
|----|--------|:---:|:---:|:---:|:---:|---------|
| — | **A (纯向量)** | 0.401 | — | 0.159 | — | 基准 |
| **A3** | **TAXONOMIC 开放** | **0.857** | **+114%** | — | — | 最高召回，CROSS_VALID ✓+~=75.6% |
| **A4** | **无目的过滤** | **0.857** | **+114%** | **0.519** | **+226%** | 最高召回，大图低于 P |
| **A8** | **固定 hop=5** | **0.857** | **+114%** | — | — | 最高召回，输出膨胀 |
| A7 | 固定 hop=2 | 0.807 | +101% | — | — | 过早截断损失 |
| A1 | 等权扩展 | 0.757 | +89% | — | — | 跨规模持续正向 |
| A5 | 无目的加权 | 0.663 | +65% | **0.428** | **+169%** | 精度-召回权衡 |
| A2 | 无跳转轴（全零） | 0.307 | -23% | **0.159** | **0%** | 大图 R@all=A 但内容置换(独家=6) |
| A6 | 随机目的 | 0.265 | -34% | **0.120** | **-24%** | LLM 推断不可替代 |

**核心发现**（按 R@all 排序）：

1. **跳转轴是所有扩展召回的关键条件（A2）**：在 216 节点上，A2 的 R@all 与 Baseline A 完全一致（0.159），表明有向图扩展是 P 产生不同于纯向量检索结果的关键源头。在 47 节点小图上 A2 反而低于 A（-23%），原因是 purpose filter（阈值 0.2）在零权重条件下仍然会滤除部分种子记忆——这是一个小图 artifact，大图上因种子语义质量更高而消失。

2. **类型化权重通过方向性阻断控制噪音，而非收敛输出量（A1）**：等权扩展的输出量（16.3）反而低于保留类型化权重的配置（A3/A4 输出 18.9）——类型化权重并未减少输出总量，而是扩大了差异化路径的探索空间。类型化权重的真实贡献体现在**方向性阻断**：TAXONOMIC 正向=0 结构性排除分类学扩散路径，这是等权扩展无法实现的。A3（TAXONOMIC 开放）在 R@all 上与 A4 并列最高（0.857），但 CROSS_VALID ✓+~=75.6%（§5.4.2）——开放后跨域联想偏向概念相关（~），TAXONOMIC 阻断的核心价值在于将跨域联想从"概念漂移"约束为"功能等价"。

3. **目的回归的两个子机制分化明确**：目的过滤（A4）是所有消融中召回最高之一（0.857），移除后 R@all 提升 114%——目的过滤在限制输出的同时牺牲了部分召回。目的加权（A5）R@all 为 0.663，低于 A4 约 0.19，效应量有限。目的加权的真实贡献是**在保持合理召回的同时略微改善排序精度**——这是一个精度-召回权衡机制。

4. **三个组件在不同维度互补**：
   - **跳转轴** → **召回深度**：A2 在大图上 R@all 与 A 持平但命中集合结构不同（独家=6）
   - **目的回归-过滤（A4）** → **召回-输出量权衡**
   - **目的回归-加权（A5）** → **精度-召回细微调节**
   - **寻峰** → **效率与充分性**：hop=2 过早使 R@all 下降（A7），但大部分有效路径在 2 跳内完成
   - **LLM 推断（A6）** → **不可替代的方向性**：随机目的在所有规模上显著劣于 A

#### 消融多指标补充

| 消融 | P@5(47) | R@all(47) | 独家命中(47) | 输出量(47) |
|------|:---:|:---:|:---:|:---:|
| Baseline A | 0.422 | 0.401 | — | 5.0 |
| A3 | 0.333 | 0.857 | 27 | 18.9 |
| A4 | 0.311 | 0.857 | 27 | 18.9 |
| A8 | 0.244 | 0.857 | 27 | 20.7 |
| A7 | 0.289 | 0.807 | 27 | 18.1 |
| A1 | 0.289 | 0.757 | 26 | 16.3 |
| A5 | 0.289 | 0.663 | 22 | 16.3 |
| A2 | 0.333 | 0.307 | 1 | 5.0 |
| A6 | 0.289 | 0.265 | 2 | 5.0 |

#### LESS 维度：P 对 A 语义误命中的排除

P 对 A 语义误命中的排除率 38%——LESS 维度的直接量化证据。以 A 基线在真实图上的误命中（A 输出中不在 expected 集内的节点，共 55 个）为"天然陷阱"，逐查询对比 P 是否输出这些节点：

| 查询 | A误命中 | P排除 | 排除率 | 说明 |
|------|:---:|:---:|:---:|------|
| 最近压力太大了 | 4 | 2 | 50% | |
| 工作太累了 | 2 | 1 | 50% | |
| 体检结果不太好，有点担心 | 3 | 0 | 0% | health 域内因果链自闭合 |
| 最近开始注意身体了 | 2 | 2 | **100%** | TAXONOMIC 结构性排除 |
| 周末想出去放松一下 | 4 | 1 | 25% | |
| 有什么适合郊游的地方 | 5 | 3 | 60% | |
| 想学Python不知道从哪里开始 | 5 | 1 | 20% | |
| 转行学编程遇到瓶颈了 | 2 | 0 | 0% | 多意图，目的向量区分力丧失 |
| 今天心情很低落 | 3 | 0 | 0% | 情绪域内因果链自闭合 |
| 有什么方法能让心情好起来 | 5 | 1 | 20% | |
| 一个人待着感觉很难受 | 3 | 0 | 0% | |
| 心情不太好 | 3 | 2 | 67% | |
| 工作压力大怎么放松 | 5 | 2 | 40% | |
| 想换个生活方式 | 4 | 3 | **75%** | |
| 最近做了哪些改变 | 5 | 3 | 60% | |
| **总计** | **55** | **21** | **38%** | |

排除率呈三类分化：(1) **健康类**（100%）——TAXONOMIC 阻断在 health 域结构性排除分类学扩散；(2) **情绪/压力类**（40-67%）——跳转轴的因果方向性约束生效；(3) **语义模糊的多意图查询**（0-20%）——多意图导致目的向量过于宽泛，目的过滤的余弦区分力丧失。

#### 跨规模消融趋势

在 47、83、216 节点三个规模上统一使用 N 线评估框架重跑：

| 消融 | 47节点 R@all (vs A) | 83节点 R@all (vs A) | 趋势 |
|------|:---:|:---:|------|
| A (基线) | 0.401 | 0.359 | — |
| A4 无目的过滤 | 0.857 (+114%) | 0.814 (+127%) | 跨规模稳健 |
| A5 无目的加权 | 0.663 (+65%) | 0.766 (+113%) | 大图上效应增强 |
| A1 等权扩展 | 0.757 (+89%) | 0.651 (+81%) | 跨规模持续正向 |

> A2 在 216 节点上为 0.159（+0%），在 47 节点上为 0.307（-23%）。A6 在 216 节点上为 0.120（-24%），在 47 节点上为 0.265（-34%）。跨规模趋势一致：A2 在大图上退化为 A，A6 在所有规模上显著劣于 A。

### 5.3 规模效应

在 216 节点自然人记忆数据集上（6 个生活场景自然交织，40 条跨场景边），运行全部方法 × 15 条查询。

#### D 基线（216 节点，15 查询）

| 方法 | P@5 | R@all |
|------|:---:|:-----:|
| Baseline A | 0.267 | 0.159 |
| Baseline B | 0.267 | 0.159 |
| Baseline C | 0.267 | 0.159 |
| Proposed (N 线) | 0.160 | 0.538 |

Proposed 在 R@all 上大幅领先 A，但 P@5 反而落后——Proposed 输出的正确记忆中约 2/3 排在 Top-5 之外。A2 消融（§5.2）提供了更干净的因果证据：P 与 A2 共享相同的输出机制与配额，仅差跳转轴，A2 的 R@all 在 216 节点上回归 A 水平——Proposed 的核心优势是**召回深度**而非排序精度。

#### 双因子驱动模型

按行为维度分解 P 相对 A 的差异（216 节点，15 条查询）：

| 查询 | A R@all | P R@all | ΔR@all | 独家 | A误命中 | Cross% |
|------|:---:|:---:|:---:|:---:|:---:|:---:|
| 心情不太好 | 0.222 | 0.889 | +0.667 | 6 | 3 | 41% |
| 今天心情很低落 | 0.154 | 0.615 | +0.461 | 6 | 3 | 64% |
| 一个人待着感觉很难受 | 0.222 | 0.778 | +0.556 | 5 | 3 | 50% |
| 工作压力大怎么放松 | 0.000 | 0.556 | +0.556 | 5 | 5 | 75% |
| 有什么方法能让心情好起来 | 0.000 | 0.500 | +0.500 | 5 | 5 | 66% |
| 最近压力太大了 | 0.091 | 0.545 | +0.454 | 5 | 4 | 59% |
| 体检结果不太好，有点担心 | 0.222 | 0.667 | +0.445 | 4 | 3 | 74% |
| 工作太累了 | 0.300 | 0.500 | +0.200 | 3 | 2 | 75% |
| 周末想出去放松一下 | 0.250 | 0.750 | +0.500 | 2 | 4 | 64% |
| 最近做了哪些改变 | 0.000 | 0.250 | +0.250 | 2 | 5 | 100% |
| 想学Python不知道从哪里开始 | 0.000 | 0.500 | +0.500 | 1 | 5 | 54% |
| 想换个生活方式 | 0.200 | 0.400 | +0.200 | 1 | 4 | 100% |
| 最近开始注意身体了 | 0.300 | 0.300 | 0.000 | 1 | 2 | 75% |
| 转行学编程遇到瓶颈了 | 0.429 | 0.286 | -0.143 | 1 | 2 | 81% |
| 有什么适合郊游的地方 | 0.000 | 0.000 | 0.000 | 0 | 0 | 91% |
| **均值** | **0.159** | **0.536** | **+0.378** | **3.1** | **3.4** | **71%** |

三个维度的强度由**种子纯度**和**图拓扑**两个因子联合驱动：

**MORE = f(跳转轴路径丰富度, A 语义覆盖缺口)**。A 的 R@all 越低，独家命中越多。三条 A_R@all=0.000 的查询独家 5/5/2——当语义检索完全失效时，跳转轴独立提供了全部召回。

**CROSS% = f(种子纯度, 图拓扑跨场景边密度)**。种子纯度越高，CROSS 越低，但纯度不是充分条件——跨场景边密度与种子纯度共同决定 CROSS 强度。MORE 与 CROSS 不独立：在有向图中，因果链和时序链天然跨越场景边界。

**LESS = g(TAXONOMIC 阻断, 目的过滤阈值)**。A3 消融（TAXONOMIC 开放）使 CROSS_VALID ✓+~ 从 P 的 51.2% 升至 75.6%——开放后跨域联想偏向概念相关（~），跳转轴的结构性阻断将跨域联想从"概念漂移"约束为"功能等价"。

**"转行学编程遇到瓶颈了"**（ΔR@all=-0.143，全表唯一负增长）展示了三个维度共存且 R@all 失效的情形。查询语义模糊（可归入学习指导、情绪缓解、职业出路三个目的），目的过滤的不对称性导致部分 expected 节点被误杀，但 P 跨域联想出了功能等价的 mood 域缓解行为（钢琴曲、治愈系动漫）。此案例暴露了 ground truth 单一域标注的固有局限——R@all 对 P 的跨域合理输出给出了负分。为此引入独立的 **CROSS_R** 指标：该案例 expected 命中 2 条，CROSS_R = 14 条功能等价的跨域合理联想，按宽松标准（✓+~）P 输出精度达 81%（30/37）。

#### 召回优势随规模单调放大

P/A 比值序列 2.1x(47)→2.8x(100)→3.7x(150)→3.4x(216)，确认单调放大趋势。216 节点略回落系全图噪音节点稀释所致。A 的 R@all 在 100 节点（0.137）和 150 节点（0.159）之间已趋于饱和，而 P 在同一区间内仍增长显著（0.388→0.591, +52%），印证了跳转轴在大图上提供了独立于语义空间的检索增量。

#### CROSS 稀释连续曲线

50→100→150→200→216 节点连续梯度追踪显示反直觉发现：CROSS 占比随规模**下降**（64.1%→51.4%）而非上升——大图提供更多域内替代路径，系统无需跨域跳跃即可找到相关记忆。规模主要放大**召回**而非**排序精度**。

### 5.4 CROSS 跨场景联想

#### 5.4.1 成因：双重条件模型

阶段三（83 节点全同质场景）中 CROSS 频率达 92%。为验证其成因，在 63 节点异构数据集（四场景：stress/mood/health/learn）上进行因果验证实验：

```
CROSS = f(种子纯度, 跳转轴路径, 目的余弦)

条件一（必要）：目的余弦 > 0.2 → 通过目的过滤
条件二（充分）：种子跨场景    → 跳转轴有扩展路径

两者缺一 → CROSS 不发生
两者兼备 → CROSS 发生
```

CROSS 不是 bug——它是种子纯度与目的回归在真实数据分布上的联合行为。当数据中的语义相似导致向量种子自然跨场景时，目的回归正确地引导了跨域联想。当数据场景在语义空间中隔离时（health/learn 与 stress/mood 无交叉种子），模型自然保持领域内聚焦。

实验揭示了 CROSS 的**单向性**（stress/mood 向外跨到 health/learn，但从不跨回）——根因在于种子跨场景散布的不对称性：stress/mood 查询的向量种子自然散布到 health/learn，而 health/learn 查询的种子自包含。**所有跨场景 cosine 均 > 0.2**，即目的过滤本身无法解释 CROSS 的不对称性——种子纯度是必要条件。

#### 5.4.2 CROSS 双指标评估

用 CROSS_OUT（域外命中数/率）和 CROSS_VALID（域外标注正确数/率）替代原有的"噪音率"指标。标注方法：deepseek-chat 模型，仅提供查询文本和记忆文本，从"是否会自然联想到"角度判断 ✓（功能等价）/~（概念相关）/✗（无关）。

**216 节点自然人记忆（P vs Baseline A）**：

| 方法 | 总输出 | CROSS_OUT | CROSS_VALID ✓ | CROSS_VALID ✓+~ |
|------|:---:|:---:|:---:|:---:|
| Proposed | 538 条 | 389 条 (72.3%) | 55 条 (14.1%) | **199 条 (51.2%)** |
| Baseline A | 75 条 | 31 条 (41.3%) | 17 条 (54.8%) | 31 条 (100%) |

P 的绝对跨域合理联想（✓+~）是 A 的 **6.4 倍**（199 vs 31）。

**消融实验（47/83/216 节点，按 CROSS_VALID ✓+~ 排序）**：

| 消融条件 | CROSS_OUT | CROSS_VALID ✓+~ | 验证了什么 |
|------|:--:|:--:|------|
| TAXONOMIC 开放 (A3) | 48.2% | **75.6%** | 开放后跨域多为 ~ 质量 |
| 固定 hop=2 (A7) | 52.8% | 72.1% | 提前截断阻止深层跨域 |
| 固定 hop=5 (A8) | 55.4% | 69.9% | 多跳增加 ~ 但不增加 ✓ |
| 等权扩展 (A1) | 73.4% | 50.8% | ≈ 完整 P (51.2%) |
| 无目的加权 (A5) | 75.3% | 45.5% | 目的加权贡献 ~5% CROSS 质量 |
| 无目的过滤 (A4) | 71.4% | 43.1% | 目的过滤对跨域质量重要 |
| **无跳转轴 (A2)** | 80.0% | **25.0%** | 无跳转轴，跨域质量崩塌 |
| **随机目的 (A6)** | 88.7% | **12.7%** | LLM 目的推断不可替代 |

核心结论：(1) **此前基于 expected 集的"噪音率"被推翻**——CROSS_VALID ✓+~ = 51.2%，过半跨域输出具有认知合理性；(2) A2 (25%) 和 A6 (12.7%) 垫底，直接量化证明了跳转轴和 LLM 目的推断对 CROSS 质量的双重必要性；(3) CROSS 不再是"问题"而是 P 的核心能力——P 的跨域合理联想是 A 的 6.4 倍。

#### 5.4.3 可控性：目的宽度 Dose-Response

通过操纵目的向量的宽度（1-3 个目的词），在 63 节点异构数据集上验证 CROSS 是否可被外部控制：

| 条件 | P@5 | MRR | CROSS频率 | vs A (0.320) |
|------|------|------|----------|-------------|
| wide（3词） | 0.300 | 0.673 | 35.4% | -6% |
| **medium（2词）** | **0.340** | **0.850** | 33.0% | **+6%** |
| narrow（1词） | 0.200 | 0.273 | 36.5% | -38% |
| manual | 0.280 | 0.540 | 35.4% | -12% |

目的宽度呈倒 U 型 dose-response 曲线，最优为 2 词。窄目的（1 词）最差——将所有缓解行为的余弦压缩到同一窄带，排序退化为纯跳转轴。目的宽度优化的价值集中在中低 CROSS 区间。

#### 5.4.4 CROSS 案例：从诊断到修复

查询「工作压力大怎么放松」（9 个期望记忆跨四域）在所有方法上 R@all=0.000。诊断发现三个根因：图拓扑断层（3 个节点不可达）、种子域偏斜（4/5 来自 work/social）、寻峰 Hop 1 锁死（伪峰值丢弃深跳节点）。渐进修复效果：补图 6 条跨域边 +3 命中（R@all=0.333）→ 种子保护 +1 命中（0.444）→ 峰值容忍带截段 +2 命中（0.667）。此案例验证了峰值容忍带的独立贡献。

### 5.5 行为维度分析

#### 5.5.1 三维度定义

| 维度 | 定义 | 机制来源 | 度量 |
|------|------|----------|------|
| **MORE** | P 命中 A 未检索到的 expected 记忆 | 跳转轴沿因果链扩展到语义空间不可达的节点 | 独家命中数 |
| **LESS** | P 避免了 A 的语义误命中 | 跳转轴方向性阻断（TAXONOMIC 正向=0）+ 目的过滤 | P 排除 A 误命中的比例（38%） |
| **CROSS** | P 输出中包含来自非查询场景的节点 | 目的向量桥接 + 跨场景边 + 种子跨场景散布 | CROSS_VALID ✓+~ = 51.2% |

三者正交：同一条查询可同时具有高独家、高 Cross% 和 A 误命中被排除。COPY 仅在 47 节点小图上出现——小图路径空间狭窄导致跳转轴扩展与向量检索重叠，是 P 检索能力退化而非"智能回退"。

#### 5.5.2 路径固化与 POINT 现象

POINT 不是 P 相对 A 的行为模式，而是跨查询的系统级收敛现象——同目的不同措辞的查询产生完全一致的输出。在 47 节点图上针对 5 条同目的情绪查询设计了三组对照实验，按效应量排序：

| 因子 | 实验设计 | 效应量 | 排序 |
|------|----------|:-----:|:--:|
| **拓扑锁定路径** | 手动添加 SOCIAL 替代边构造多分叉 vs 单一路径 | Jaccard 0.69→0.44（**-25.3%**） | **1** |
| **目的加权排序** | A5 消融 vs 标准 P，对比 R@all 变化 | 召回 +39% / 精度下降 | **2** |
| **目的宽度** | 1/2/3/5 词扫描，所有指标不变 | **零** | **3** |

POINT 由三重锁定驱动（拓扑主导），随规模增长自然瓦解——在 83 节点上种子发散导致路径分化，固化瓦解。

#### 5.5.3 典型案例

**LESS：语义误命中过滤（83节点）**。查询"改了 18 版甲方还不满意，想死"（设计师场景）：A 的 Top-5 全部是描述挫败的"原因"节点（P@5=0.000），P 沿 CAUSAL 正向扩展至缓解行为（陶艺放松、公园散步），P@5=0.400。LESS 的本质是**语义误命中的排除**：跳转轴的方向性约束将检索从"找语义相似的文本"校正为"找功能等价的行为"。

**MORE：独家缓解方法（47节点）**。查询"工作太累了"："咖啡"/"散步"/"咖啡馆"与查询无语义关联，纯向量检索无法命中。P 通过跳转轴沿 s1(加班)→s2(咖啡)、s11(睡眠不足)→s7(散步)→s4(咖啡馆) 的有向因果链找到这些缓解行为。

**CROSS：跨场景目的联想（83节点）**。查询"改了 18 版甲方还不满意"：P 的目的推断为"宣泄情绪、寻求安慰"。跳转轴把设计师的缓解行为（陶艺、公园）和撰稿人的缓解行为（写作工作坊、散步听古典）都拉了出来——两者共享同一个目的向量，功能等价。

## 六、讨论

### 6.1 设计性质与局限

**嵌入模型质量的影响**。bge-large-zh-v1.5 对抽象目的词的编码存在语义偏差——"喝咖啡"与"缓解"的余弦仅 0.2-0.3，影响了目的过滤的精度。但跳转轴的贡献具有结构性，更强的嵌入模型不会消解其价值：TAXONOMIC 阻断在更强 embedding 下反而更重要（噪音更汹涌）；多跳因果链（如"压力大"→"安静咖啡馆"）不存在语义维度共享，任何静态 embedding 无法编码 3-hop 因果链；类型化方向信号不在语义空间内，独立于 embedding。

**跳转轴权重的设计性质**。权重基于扩散激活理论手工设计，消融实验验证了类型化权重的正向贡献和 TAXONOMIC 阻断的必要性，但具体数值的序数关系（如 CAUSAL > SCENARIO > SEQUENCE）有理论支撑，而精确标定（0.9 vs 0.8 vs 0.7）在距离衰减和目的加权的双重压缩下差异极小（< 0.02），几乎不可通过实验区分。手工设计 + 消融验证是当前约束下的可行策略。

**数据集人工构造**。所有实验数据集为手写 YAML，未包含真实对话日志中的记忆。控制实验通过精确标注类型与关系方向排除噪音与歧义，适合因果推理与机制验证，但无法反映真实使用中自然语言理解、实体消歧和关系抽取等前置环节引入的误差。本文的目标是机制验证，将"该模型能否在真实数据上复现同等效果"列为最重要的未来工作方向。

**统计效力**。全篇实验以 9-54 条查询规模为主，未进行统计显著性检验。原因在于小样本下统计检验的效力过低，且实验目标为机制验证而非假设检验——消融实验通过存在性证据（A2 归零、A6 反转）和跨规模一致性（47→83→216 节点）构建因果推理链，而非依赖统计推断。

**CROSS 认知合理性的评估方法**。多 LLM 交叉标注（v1 目的视角 39%→v2 联想视角 80%, Fleiss' κ=0.25）揭示了一个方法论陷阱：提供目的向量给 LLM 做标注会造成循环论证——用语义匹配衡量超越语义匹配的系统。v2 修正为联想视角后，跨域合理率跃升至 67-80%。Fleiss' κ=0.25 反映跨域联想判断本身的认知主观性，与记忆系统"不追求唯一正确答案"的设计哲学一致。

### 6.2 检索-排序-生成的管道价值

本部分的全部实验以 R@all 为主指标，聚焦检索阶段的召回深度。然而，Proposed 方法的下游价值不仅体现在"找到更多正确记忆"，还体现在**改变了候选池的质量结构**。

**候选集的关联密度更高**。P 的输出节点之间存在有向图边连接（因果链、时序链、情境归属等），而非彼此孤立的语义近邻。这对 LLM 重排序是重要信号——重排序模型可以沿着图结构理解"这条记忆为什么被检索到"，而非仅凭 embedding 分数判断相关性。

**种子效率更高**。P 仅需 k=5 的向量种子即可展开有向联想链，在大图上通过跳转轴独立产生语义外增量（消融 A2 为因果证据）。纯向量检索要达到同等召回需要将 k 拉高到 20 以上——P 不需要扩大种子 k 就能产生有效的候选扩展。

Proposed 方法将检索管道从"广撒网→LLM 自己挑"改进为"精准播种→有向扩展→结构感知排序→高质量上下文"。论证这一管道改进的端到端效果（检索→排序→生成质量的完整链路）需要与具体的 LLM 重排序模型和生成评测任务配合，列为未来工作。

## 七、本部分小结

本部分提出了目的驱动的联想记忆检索模型，将记忆检索从"语义匹配"重新定义为"有向的联想搜索"。方法的核心由三个机制构成——跳转轴提供方向性、目的回归保持不偏离、峰值容忍带替代固定截断。

实验证据表明，Proposed 方法的核心优势在于**召回深度**。消融实验确立了各组件的维度分化：跳转轴是召回深度的关键条件（A2 在大图上退化为纯向量水平，独家命中归零），TAXONOMIC 阻断将开放后的干扰率从 44% 降至零；目的回归的子机制分工明确——过滤控制召回-输出量权衡，加权提供精度微调。A2 消融（无跳转轴→R@all 回归 A 水平）是最干净的因果证据：P 与 A2 共享相同的输出机制与配额，仅差跳转轴，直接证明了图结构信号的独立贡献。召回优势随规模放大：P/A 比值从 47 节点的 2.1 倍扩大至 216 节点的 3.4 倍——纯语义检索在大图上退化得更快，而跳转轴的图结构信号提供了独立于 embedding 质量的检索能力。代价是 Top-5 排序精度低于纯向量方法，Proposed 适用于"多轮对话中提供丰富记忆上下文"的场景。

三个行为维度（MORE/LESS/CROSS）揭示了跳转轴与目的回归在不同方面的差异化表现。系统性 CROSS_VALID 标注（§5.4.2）确认了 P 的跨域合理联想是 A 的 6.4 倍——CROSS 不是缺陷而是 P 的核心能力。LESS 维度以 A 的天然误命中为评估对象，确认了 P 排除语义误命中的 38%，TAXONOMIC 阻断是其中的机制级证据（健康类排除率 100%）。多 LLM 交叉标注揭示了一个方法论陷阱——将记忆检索评估为"需求回应"会系统性低估跨域联想的合理性，评估应基于联想自然度而非答案正确性。

本部分的核心启示是：在长期记忆检索中，知道"往哪个方向找"和"为什么找"比"找得多近"更重要。

需要强调的是，本部分所有实验均在人工构造、精确标注节点类型与关系方向的数据集上完成。在真实场景中，记忆文本的自动结构化、实体消歧与关系抽取都将引入额外误差——从机制验证到生态效度评估的跨越是本文最重要的后续方向。

---

# 总结论：从前置数据到回复的完整理论闭环

本文通过两个相互依赖的部分，将对话系统的长期记忆管理统一为 Ariadne 系统的完整理论闭环。

**第一部分（前置数据管理）** 的核心结论是：将记忆图谱构建从"一次性 NLP 抽取"重新定义为"LLM 持续自主维护的数据库"。其设计哲学是"让 LLM 做最难的部分"——事实抽取、类型标注、矛盾检测、指代消解等能力随 LLM 进步零成本增益，而非固化为管道式 NLP 的技术债。增量可纠错的 CRUD 操作集（create/update/fix_type/deprecate + create/delete）、三层上下文、异步批量调度器、边衰减遗忘与"城市-高速公路"自组织共同构成了一个可持续演化的记忆数据库。

**第二部分（检索理论）** 的核心结论是：将记忆检索从"语义匹配"重新定义为"有向的联想搜索"。跳转轴提供方向性、目的回归保持不偏离、峰值容忍带替代固定截断，三机制协同使检索的核心优势落在**召回深度**上——知道"往哪个方向找"和"为什么找"比"找得多近"更重要。

两部分的理论闭环体现在两个方向：

1. **下游依赖**：前置数据管理产出的图谱质量直接决定检索阶段的上限。节点类型与边方向的标注精度决定跳转轴的方向性信号；`fix_type`/`delete` 既是维护功能，也是检索质量的持续保障；StoryRank（第一部分 §6.4）的关系类型（因果链、偏好、情境等）直接依赖 DBA 图谱的边结构。

2. **上游反馈**：检索实验的四个核心发现（MORE/CROSS/LESS/CROSS% 随规模下降）反过来验证了前置数据管理的设计正确性——"只连有联想价值的边"（城市-高速公路模型）、"边衰减而非时间衰减"的遗忘机制，都是检索行为在数据管理侧的必要前提。

两个部分共享两个贯穿始终的方法论立场：其一，**将最难的部分外包给 LLM**，使系统能力随模型进步自然增长；其二，**所有实验在人工构造、精确标注的数据集上完成**，机制验证是当前目标，真实对话日志上的生态效度评估是共同的首要后续方向。

### 全链路工程化状态（2026-08-12 更新）

本文描述的理论系统已完成生产级工程化封装（详见 [DBA工程化落地计划](DBA工程化落地计划.md)）：

- **DBA 前置数据管理**：LLM 自主维护记忆图谱，三层上下文 + CRUD 操作指令集 + 异步批量调度器，通过 MCP `dba_add_conversation` 封装为标准工具
- **检索链路**：跳转轴 + 目的回归 + 峰值容忍带终止，通过 `dba_query_memory` 暴露为 MCP 工具
- **持久化**：图谱 YAML 自动保存 + FAISS 向量索引 + 调度器/构建器运行时状态，支持统一 `checkpoint`/`restore` 和定时自动检查点
- **MCP Server**：6 个标准 MCP Tool（`dba_add_conversation`、`dba_query_memory`、`dba_inspect_graph`、`dba_intervene`、`dba_checkpoint`、`dba_get_stats`），支持 stdio + SSE 双传输，任何遵循 MCP 的 LLM Agent 均可直接调用
- **3D 可视化面板**：基于 `3d-force-graph` + `Three.js` 的 3D 力导向图，支持图层过滤、聚焦模式、模糊搜索、拖拽固定、CRUD 控制面板（详见 [DBA可视化面板设计](DBA可视化面板设计.md)）
- **人工干预**：前端 CRUD 面板 + MCP `dba_intervene` 双通道，所有操作自动持久化到 YAML

这些工程化工作将本文的理论原型验证系统提升为可部署的完整产品：`api_server.py`（HTTP REST + 3D 面板服务）、`mcp_server.py`（MCP 协议封装）、`renderer.py`（离线 HTML 生成）。

---

*本文由《前置数据管理：LLM DBA 自动化记忆图谱构建》（基于三模块实现 + 9 项集成验证）与《目的驱动的联想记忆检索模型——检索理论部分》（基于六阶段 15+ 实验线、8 组消融、CROSS 双指标评估）合并而成，构成完整的「对话日志 → 记忆图谱 → 联想检索 → 精排回复」理论闭环。*
